name: publish-central-server

on:
  workflow_dispatch:
  push:
    tags:
      - "central-server-v*"

jobs:
  build-win-x64:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Publish central server package
        shell: pwsh
        run: .\scripts\publish_central_server.ps1

      - name: Upload central server artifact
        uses: actions/upload-artifact@v4
        with:
          name: central-server-win-x64
          path: |
            dist/central-server-win-x64/*.zip
            dist/central-server-win-x64/*.sha256

      - name: Upload plugin lean artifact
        uses: actions/upload-artifact@v4
        with:
          name: plugin-lean
          path: |
            dist/plugin-lean/*.zip
            dist/plugin-lean/*.sha256

      - name: Upload plugin bundled artifact
        uses: actions/upload-artifact@v4
        with:
          name: plugin-bundled-win-x64
          path: |
            dist/plugin-bundled-win-x64/*.zip
            dist/plugin-bundled-win-x64/*.sha256

      - name: Upload release assets
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/central-server-win-x64/*.zip
            dist/central-server-win-x64/*.sha256
            dist/plugin-lean/*.zip
            dist/plugin-lean/*.sha256
            dist/plugin-bundled-win-x64/*.zip
            dist/plugin-bundled-win-x64/*.sha256
          generate_release_notes: true
