## 🧩 Godot .NET MCP v1.2.1: Verified Editor UI Automation

この version line では editor UI automation を改善し、client が次の workflow に進む前に Godot editor 内の visible state を待てるようにします。Agent は既存の editor control で操作したあと、期待する UI condition が実際に現れたかを検証できます。

<p align="center"><a href="https://github.com/LuoxuanLove/godot-dotnet-mcp/blob/v1.2.1/docs/en/process/release-notes/release-notes-v1.2.1.md">English</a> | <a href="https://github.com/LuoxuanLove/godot-dotnet-mcp/blob/v1.2.1/docs/zh-CN/流程/发布说明/发布说明-v1.2.1.md">简体中文</a> | <a href="https://github.com/LuoxuanLove/godot-dotnet-mcp/blob/v1.2.1/docs/ja/プロセス/リリースノート/リリースノート-v1.2.1.md">日本語</a> | <a href="https://github.com/LuoxuanLove/godot-dotnet-mcp/blob/v1.2.1/docs/ko/프로세스/릴리스-노트/릴리스-노트-v1.2.1.md">한국어</a></p>

### ✨ 実際の UI 状態を待機

`system_editor_control` に `wait_for_ui` を追加しました。control の存在、可視性、text matching、enabled / disabled state を有界にポーリングできます。成功時は一致した control summary と timing detail を返し、timeout 時は最後に観測した payload を返すため、client は locator や次の action を調整できます。

### 🔧 より安全な automation loop

新しい wait action は menu、popup、click、hover、text entry control と組み合わせて使え、OS mouse automation は不要です。editor dialog を開く、setting を filter する、一時 panel を確認する、といった workflow を Godot 内で検証しやすくします。

Editor UI guidance では automation order を明確にしました。semantic workflow と navigation を最優先し、control-level の focus、text、value、popup action を次に使い、control-local mouse / pointer event は fallback としてのみ使います。Agent は座標に進む前に、より高レベルな経路を選べます。

Resource / prompt diagnostics は、resource output を報告する前に mixed-case URL credential、bearer / API-key variant、nested metadata secret を redact します。troubleshooting evidence は automation loop に有用なまま、summary に credential を露出しにくくなります。

### 🪟 Floating Window Evidence

`system_editor_control` は `get_popup` と `capture_popup` により、可視の floating popup / window surface を読み取り、crop した evidence を取得できるようになりました。client は button、menu item、text field、close action を選ぶ前に modal editor window や PopupMenu の状態を直接確認できます。

`system_settings_dialog` は settings surface を capture するとき、この evidence path を自動的に使います。現在の Project Settings / Editor Settings workflow が対応する popup や editor window を観測できる場合、capture result は選ばれた backend と target path を示し、より狭い surface が使えない場合だけ broader editor screenshot に fallback します。

`system_editor_evidence` は editor、control、popup、active-dialog、auto surface の evidence-aware capture entry を提供します。結果は requested surface、actual backend、target path、visible popups、fallback reasons、degraded state を示すため、Agent は screenshot が現在の task surface を証明しているのか、より広い fallback view だけなのかを判断できます。

### 🧭 Configuration Navigation and Evidence

`system_settings_dialog` は Project Settings と Editor Settings 向けの high-level workflow を追加します。client は editor menu から settings surface を開き、dialog の可視化を待ち、候補 setting row を検索し、返された result に focus し、evidence を capture して、setting value を直接書き込まずに surface を閉じられます。

この workflow は `list_rows` で読み取り専用 row model も返せるようになり、可視 settings-like control を保守的な confidence と evidence 付きで要約してから、必要な value read、write、assertion を選べます。

`list_tabs` と `activate_tab` で settings tab を list / activate でき、`open(tab=...)` も使えるため、座標クリックなしで Project Settings / Editor Settings の tab 間を移動できます。

この settings workflow は visible category tree item を list し、row inspection の前に一意の category に focus できます。Project Settings / Editor Settings navigation が raw control enumeration に依存しにくくなります。

さらに `read_value` で一意に一致した可視 row の現在値を読み取り、row と value-control の evidence とともに text、bool、number、enum の型付き payload を返せます。setting value は引き続き直接書き込みません。

Project configuration evidence は input map も対象にします。Agent は `system_project_configure` から個別の project input action を読み取り、deadzone と具体的な key、mouse、controller binding を確認できます。これにより gameplay や editor shortcut を変更する前に、実際の project configuration に基づいて input map を確認できます。

Project configuration は `system_project_configure(action="list_export_presets")` で export preset も一覧できます。response は preset name、platform、runnable state、filter、script export mode、export path の形を要約し、sensitive option key と local absolute export path は redact します。

Settings update source と branch selector は、長い option list を有界の popup 内に収めるようになりました。Agent は menu が editor workspace を覆うことなく、利用可能な ref をすべて確認できます。

Client は `resolve_row` により、値の読み取りや後続 UI 操作の前に一意の可視設定行を解決できます。response には row path、confidence、selector evidence、ambiguity diagnostics が含まれ、読み取り専用のまま動作します。

Agent が一致した value editor へ keyboard focus だけを移したい場合は、`focus_value` が value control を focus し、設定を変更せずに focus evidence を返します。

対応している一意の可視 row は、`set_value` で text、number、bool control を編集できます。この workflow は editor UI control 経由で書き込み、操作後に row を再観測するため、client は click や text edit が成功したと仮定せずに変更後の値を確認できます。

`run_task` は、この settings chain を 1 つの trusted operation としてまとめます。settings surface を開いて絞り込み、一意の row を解決し、変更前の値を読み取り、対応している値を必要に応じて書き込み、期待する結果を検証し、capture evidence を添付できます。selector が ambiguous、hidden control write が要求されている、confidence が低すぎる、または値が変わる前に required capture evidence を作れない場合、write は interaction 前に拒否されます。

Agent は読み取り専用の `verify_value` check も使い、期待する text、bool、number、enum 値を一意に一致した可視 row と比較できます。settings workflow に、UI action の前後で値を書き換えない assertion step を追加できます。

Inspector automation には、`system_inspector` を通じた専用の high-level workflow が追加されました。client は可視 Inspector property model を列挙し、一意の property を解決し、現在の typed value を読み取り、value editor に focus し、対応している text / number / bool editor を検証付きで書き込み、property evidence を capture し、現在の edited object または準備済み node / resource target に対して locate / read / set / verify / capture task をまとめて実行できます。

Inspector workflow は、hidden-control write、ambiguous selector、disabled value editor、未対応の complex editor を推測せずに拒否します。これにより Agent は raw editor control enumeration や coordinate-based input に落ちる前に、property-level の信頼できる経路を使えます。

### 🔎 検索できる Tool Discovery

`system_tool_catalog` は、現在の tool catalog を読み取り専用で検索する入口です。client は query、category、domain で検索し、各 tool が一致した理由、公開する action と parameter、visible state、enabled state を確認できます。

これにより、profile、plugin domain tool、user tool state が変わったあとでも、configuration / editor workflow に入り直しやすくなります。Agent は古い tool name の記憶に頼らず、次の verified action の前に適切な system、project、editor、runtime、script、user-facing tool を再発見できます。

Native MCP discovery も resource と resource template の metadata をローカライズするようになりました。client が `resources/list` または `resources/templates/list` を呼ぶと、resource name と description は現在の plugin language に従い、URI と template は安定したまま維持されます。

### 🧰 より分かりやすい User Tool 名

User tool listing と compatibility report は、script-declared name、正規化後の internal name、公開 MCP tool name を同時に表示するようになりました。scaffold preview も公開名を最初から示し、旧式の `user_` prefix 付き declaration には review warning を出すため、client が tool の表示名を推測する必要が減ります。

新しい scaffold は、base tool type を global script-class registration に依存しないため、headless catalog scan でもより安定して読み込まれます。

### 🧪 Read-Only Scene Inspection

`system_scene_inspect` は、scene check の読み取り専用 entry を 1 つ提供します。`validate` は loadability と reference issue の高速確認、`analyze` は node、binding、script の深い詳細、`full` は 2 つの payload を分けたまま 1 つの response で返したい workflow に使えます。

### 🛠 Plugin Maintenance Workflow

`system_plugin_maintenance` は、plugin status の確認、lifecycle reload の scheduling、update progress の確認、update source の選択、plugin update flow の開始を 1 つにまとめた maintenance entry です。この tool は追加の high-level wrapper であり、既存 client は lower-level の reload / update tool を直接組み合わせる使い方も継続できます。

### 🧭 Project Runtime Lifecycle

Project runtime control は `system_project_lifecycle(action=start|stop)` を canonical な high-level entry として使うようになりました。start、marker validation、auto-stop、explicit stop、foreground-window fallback guidance、runtime capability context が、個別の run / stop tool 名ではなく 1 つの lifecycle surface にまとまります。

古い project run / stop tool 名を呼んでいた client は、`system_project_lifecycle(action=start)` と `system_project_lifecycle(action=stop)` に移行してください。lifecycle という名前により、editor automation flow は単発の launch command ではなく、制御可能な runtime session として扱いやすくなります。

### ✅ Compatibility and Upgrade Notes

Project runtime control surface は `system_project_lifecycle(action=start|stop)` を使うようになりました。以前の project run / stop tool 名を使っている client は lifecycle entry に移行してください。既存の editor、settings、project configuration workflow は引き続き利用できます。

既存の scene validation / analysis tool は引き続き利用できます。`system_scene_inspect` は、1 つの high-level scene inspection surface が必要な client 向けの追加 combined route です。

`system_plugin_maintenance` は追加の high-level wrapper です。既存の `system_plugin_reload` と `system_plugin_update` call は引き続き supported です。
