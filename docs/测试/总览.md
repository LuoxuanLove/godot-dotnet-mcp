# 测试总览

本文档汇总 `godot-dotnet-mcp` 当前测试模块的整体结构、覆盖范围、已知边界和后续演进方向。

测试模块当前只有一层：

1. `Godot Headless Harness`

---

## 当前状态

### 已落地的部分

- `tests/godot_plugin_harness_fixture/tests/` 已有多个 headless case，覆盖插件运行时、工具装载和路由行为。
- 插件侧已经补入第一轮稳定测试 seam：
  - `mcp_editor_lifecycle_endpoint.gd`
  - `mcp_editor_lifecycle_action_service.gd`
  - `mcp_editor_lifecycle_state_builder.gd`
  - `mcp_http_request_router.gd`
  - `mcp_http_request_decoder.gd`
  - `mcp_http_connection_state.gd`
  - `mcp_http_transport_service.gd`
  - `mcp_http_service_bundle.gd`
  - `mcp_http_response_service.gd`
  - `mcp_json_rpc_router.gd`
  - `mcp_protocol_facts.gd`
  - `mcp_tools_api_service.gd`
  - `mcp_runtime_fallback_store.gd`
  - `mcp_runtime_reply_service.gd`
  - `mcp_runtime_control_reply_resolver.gd`
  - `mcp_http_server.gd` 的公共测试入口
  - `mcp_runtime_bridge.gd` 的公共 fallback / command capture 入口
- `validate-refactor.yml` 已接入 plugin harness 作为 CI 主线。

### 当前已验证结果

- Plugin headless harness：多 case 通过
- Plugin headless harness 退出无泄漏告警

当前 plugin harness 覆盖的 case 包括：

- `runtime_bridge_invalid_action_fallback`
- `runtime_control_contracts`
- `runtime_control_request_coordinator_contracts`
- `runtime_control_reply_resolver_contracts`
- `runtime_fallback_store_contracts`
- `runtime_reply_service_contracts`
- `user_tool_watch_service_contracts`
- `user_tool_service_contracts`
- `script_tool_executor_contracts`
- `script_edit_service_contracts`
- `node_tool_executor_contracts`
- `animation_tool_executor_contracts`
- `physics_tool_executor_contracts`
- `scene_tool_executor_contracts`
- `debug_tool_executor_contracts`
- `editor_tool_executor_contracts`
- `lighting_tool_executor_contracts`
- `geometry_tool_executor_contracts`
- `filesystem_tool_executor_contracts`
- `project_tool_executor_contracts`
- `material_tool_executor_contracts`
- `ui_tool_executor_contracts`
- `particle_tool_executor_contracts`
- `resource_tool_executor_contracts`
- `shader_tool_executor_contracts`
- `tilemap_tool_executor_contracts`
- `signal_tool_executor_contracts`
- `group_tool_executor_contracts`
- `audio_tool_executor_contracts`
- `navigation_tool_executor_contracts`
- `plugin_bootstrap_contracts`
- `plugin_dock_coordinator_contracts`
- `plugin_runtime_coordinator_contracts`
- `config_feature_config_workflow_contracts`
- `client_config_serializer_contracts`
- `client_config_inspection_service_contracts`
- `client_config_file_transaction_contracts`
- `client_config_launcher_adapter_contracts`
- `http_server_contracts`
- `http_request_router_contracts`
- `http_request_decoder_contracts`
- `http_transport_service_contracts`
- `http_response_service_contracts`
- `json_rpc_router_contracts`
- `editor_lifecycle_action_service_contracts`
- `editor_lifecycle_state_builder_contracts`
- `system_project_executor_contracts`
- `system_script_executor_contracts`
- `system_runtime_impl_contracts`
- `system_index_impl_contracts`
- `tool_loader_contracts`
- `server_tab_model_projection_contracts`
- `tools_tab_interaction_support_contracts`
- `tools_tab_search_service_contracts`
- `tools_tab_preview_builder_contracts`
- `roslyn_parsing_contracts`
- `plugin_roslyn_service_contracts`
- `csharp_tool_engine_contracts`
- `external_host_removal_audit`

---

## 当前架构判断

### 已经合理的部分

- 测试已经从"单个 smoke 脚本"演进为独立的 harness 层，不再把所有验证都堆在一个入口里。
- 插件测试已经开始使用 fake collaborator 验证 `impl_runtime`、`impl_index` 等模块，说明不是所有测试都依赖真实运行时。
- Config 页运行时链已经补入 `serializer / inspection / file transaction / launcher adapter` 四类轻量契约。

### 当前仍需收口的部分

#### 1. 插件 contract tests 已脱离私有方法名，但 seam 仍可继续收口

当前 `http_server_contract_test.gd` 与 `runtime_bridge_contract_test.gd` 已改为走公共入口，不再直接依赖下划线方法。  
这一轮已经显著降低了"内部方法改名即测试碎裂"的风险，而且 runtime fallback / reply 行为也已经从 bridge 节点里抽出第一轮独立 helper；但 `mcp_http_server.gd` 与 `mcp_runtime_bridge.gd` 仍然持有一部分测试 seam，本质上还可以继续向更独立的 helper 收口。

#### 2. CI 中环境依赖测试仍未完全硬门禁

当前 `validate-refactor.yml` 会运行 plugin harness，但允许在缺少 Godot 可执行文件时跳过。  
这意味着插件测试层已经接入 CI，但还没有完全升级为"不可跳过的质量门"。

---

## 当前推荐阅读顺序

如果目标是理解测试模块整体状态，建议顺序如下：

1. [插件Headless测试.md](插件Headless测试.md)
2. [Smoke与CI.md](Smoke与CI.md)

---

## 设计原则

当前测试模块的演进原则如下：

- 测试能力是一级架构模块，不再附属于 smoke 脚本或人工真机验证。
- 尽量通过稳定 seam 测试对外行为，而不是依赖私有实现细节。
- 大文件先拆再改，测试文件也遵循相同规则。
- 环境依赖用例必须单独声明门禁策略，不能和快测层混成一类。

---

## 下一步方向

### 第一优先级

- 继续把 `mcp_http_server.gd` 的稳定测试 seam 从 server 节点内部往外收口：
  - `McpRequestRouter`
  - `EditorLifecycleEndpoint`
  - `ToolListResponseBuilder`
- 继续把 `mcp_runtime_bridge.gd` 的稳定测试 seam 从 bridge 节点内部往外收口：
  - `RuntimeCommandAdapter`
  - `RuntimeFallbackStore`
  - `RuntimeReplyWriter`
  - `RuntimeCaptureExecutor / RuntimeInputExecutor / RuntimeStepExecutor`
- `runtime_control_service.gd` 的第二轮 seam 外提已落地，当前协作者为：
  - `SessionSelector`
  - `EditorErrorMapper`
  - `RuntimeReplyResolver`
  - `RequestCoordinator`
- 这一层后续优先补的是 `runtime_session_lost`、bridge unavailable 和 request cleanup 的负例，而不是再引入新的 reply waiter 抽象。

### 第二优先级

- 继续把 plugin contract tests 收口为更清晰的 fixture / assertion / helper 分层。

### 第三优先级

- 将测试矩阵继续分层为：
  - `Fast Required`
  - `Integration Required`
  - `Environment Required`

---

## 结论

测试模块已经从"起步阶段"进入"正式治理阶段"。  
当前最重要的任务，不是继续无序增加 case，而是先把 harness 测试之间的边界、夹具清理和测试 seam 做稳。

只有在这些基础稳定后，后续测试覆盖的扩展才不会把测试模块重新推回高耦合和超大文件状态。
