# Godot.NET MCP 文档概述

本文档目录用于说明 `addons/godot_dotnet_mcp` 的架构、界面操作、工具调用规范、Godot.NET 分析能力、迁移注意事项与测试结论。文档默认面向两类读者：

- 需要在 Godot 编辑器内使用 Dock、切换工具集、生成 MCP 客户端配置的使用者
- 需要继续维护 `plugin.gd`、`plugin/runtime/mcp_http_server.gd` 与 `tools/*.gd` 的开发者

---

## 阅读顺序

如果目标是“先用起来”，建议按以下顺序阅读：

1. [架构/配置与界面.md](架构/配置与界面.md)
2. [架构/安装与发布.md](架构/安装与发布.md)
3. [架构/服务与路由.md](架构/服务与路由.md)
4. [模块/工具系统.md](模块/工具系统.md)

如果目标是“继续开发或排障”，建议按以下顺序阅读：

1. [架构/概述.md](架构/概述.md)
2. [架构/服务与路由.md](架构/服务与路由.md)
3. [模块/脚本与场景分析.md](模块/脚本与场景分析.md)
4. [模块/NET支持.md](模块/NET支持.md)
5. [迁移/迁移说明.md](迁移/迁移说明.md)

## 常见工作流

### 1. 启动本地 MCP 服务

1. 在 Godot 编辑器中启用插件。
2. 打开 `MCPDock > 服务器`。
3. 检查端口、自动启动和调试日志开关。
4. 点击“启动”或保持自动启动。
5. 通过 `http://127.0.0.1:3000/mcp` 或当前端口对应地址接入客户端。

对应说明详见 [架构/配置与界面.md](架构/配置与界面.md) 与 [架构/服务与路由.md](架构/服务与路由.md)。

### 2. 裁剪工具暴露范围

1. 打开 `MCPDock > 工具`。
2. 选择内置 profile 或自定义 profile。
3. 按 domain、category、tool 三层缩进结构开关启停工具。
4. 观察启用数变化，并确认服务端禁用列表已同步。

对应说明详见 [模块/工具系统.md](模块/工具系统.md)。

### 3. 查看连接配置

1. 打开 `MCPDock > 配置`。
2. 查看可直接写入的桌面客户端 JSON 配置。
3. 检查当前服务地址、配置内容和写入目标路径。
4. 如需直接写入本机配置文件，使用一键写入按钮。

补充：

- 语言切换菜单会显示“自称（当前语言译名）”格式，便于在非母语界面下识别目标语言。
- 插件 UI 文本已统一走 localization key；如果界面出现英文回退，优先检查语言资源是否缺 key。

对应说明详见 [架构/配置与界面.md](架构/配置与界面.md)。

### 4. 排查 C# 导出绑定问题

1. 用 `script_inspect` 或 `script_exports` 读取脚本导出成员。
2. 用 `scene_bindings` 分析当前场景或指定场景的绑定情况。
3. 用 `scene_audit` 获取结构化问题列表。
4. 如需回查节点路径，再配合 `scene_hierarchy` 或 `node_query`。

对应说明详见 [模块/脚本与场景分析.md](模块/脚本与场景分析.md) 与 [模块/NET支持.md](模块/NET支持.md)。

## 文档结构

### [架构/](架构/)

描述插件分层、协议路由、配置生成、界面分工和运行时状态。

| 文档 | 内容 |
|---|---|
| [架构/概述.md](架构/概述.md) | 插件定位、分层结构、核心对象与运行时职责 |
| [架构/安装与发布.md](架构/安装与发布.md) | 安装方式、启用步骤、发布包结构与分发建议 |
| [架构/服务与路由.md](架构/服务与路由.md) | MCP 服务入口、HTTP/MCP 路由、工具注册、错误结构与禁用工具策略 |
| [架构/配置与界面.md](架构/配置与界面.md) | Dock 页签操作、设置持久化、配置生成、一键写入与 UI 缩放策略 |

### [模块/](模块/)

描述工具系统的分类、调用契约、脚本/场景分析流程和 Godot.NET 支持边界。

| 文档 | 内容 |
|---|---|
| [模块/工具系统.md](模块/工具系统.md) | 工具分类、调用约定、写后验证、路径规则、返回字段与共享 helper |
| [模块/脚本与场景分析.md](模块/脚本与场景分析.md) | `script_*`、`scene_bindings`、`scene_audit` 的操作方法与实现路径 |
| [模块/NET支持.md](模块/NET支持.md) | C# 静态分析范围、导出识别、场景绑定检查与非目标能力 |

### [迁移/](迁移/)

记录旧入口与新入口的映射、返回结构变化与 UI 架构变化。

| 文档 | 内容 |
|---|---|
| [迁移/迁移说明.md](迁移/迁移说明.md) | 工具收敛、返回字段迁移、场景化 Dock 重构与兼容要求 |

### [测试/](测试/)

记录测试策略、确认过的缺陷与回归覆盖点。

| 文档 | 内容 |
|---|---|
| [测试/测试策略.md](测试/测试策略.md) | 本地冒烟测试的范围、分层方法与通过标准 |
| [测试/缺陷清单.md](测试/缺陷清单.md) | 2026-03-10 缺陷清单、修复结果与回归说明 |

## 文档使用约束

- 文档默认以 2026-03-10 之后的重构版本为准，旧版大 `plugin.gd` 动态拼装 UI 的描述已不再适用。
- 如果修改了工具返回字段、设置键名、profile 文件格式或客户端配置模板，必须同步更新相应章节。
- 如果修改了界面结构、按钮行为或缩放策略，必须同步更新 [架构/配置与界面.md](架构/配置与界面.md)。
- 如果修改了工具收敛规则、路径规则或共享 helper，必须同步更新 [模块/工具系统.md](模块/工具系统.md) 与 [迁移/迁移说明.md](迁移/迁移说明.md)。
