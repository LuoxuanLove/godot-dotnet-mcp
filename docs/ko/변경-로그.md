# 변경 로그

이 프로젝트의 중요한 변경 사항은 모두 여기에 기록합니다.

이 문서는 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 형식을 따르며, [Semantic Versioning](https://semver.org/lang/zh-CN/spec/v2.0.0.html)도 함께 따릅니다.

## [Unreleased] ([2.0.0])

대상 버전은 2.0.0입니다.

### Added

- v2.0 첫 .NET Companion contract library와 pure contract runner를 추가해 project sessions, capability-gated static/headless mode, 명시적 editor-live upgrade, project/session-scoped tool calls를 다룹니다.
- issued/last-used/expiry metadata, lease renewal, stop/revoke behavior, stale-session rejection을 포함한 explicit project session lifecycle controls를 추가했습니다.
- Godot을 시작하지 않고 Godot project status, 발견된 `.csproj` files, plugin directory presence, editor-live를 사용할 수 없는 이유를 보고하는 Companion static/headless project inventory analyzer를 추가했습니다.
- project restore 또는 build 없이 SDK, target frameworks, package references, project references, compile include/remove, Godot SDK/package hints를 보고하는 unevaluated static .NET workspace graph를 추가했습니다.
- editor를 열지 않고 text `.tscn` / `.tres` files의 external resources, sub-resources, `uid://` markers, `res://` usages, missing references, unsupported binary `.res` files를 기록하는 static resource reference graph를 추가했습니다.

### Fixed

- static resource graph를 수정해 `preload()` usage가 동시에 `load()` usage로 기록되지 않게 하고, 손상된 external-resource fallback path와 reparse point를 통과하는 `res://` reference를 진단하도록 했습니다.
- `supports_live_editor_state`가 없거나 compatible v2 plugin version이 없거나 missing, malformed, older-major, newer-major, explicit version-mismatch plugin version을 보고하는 bridge status의 editor-live upgrade를 거부하도록 했습니다.
- Bridge status `plugin_version` schema와 runtime compatibility parser를 맞춰 optional `v` prefix, prerelease/build metadata, whitespace rejection 규칙을 일치시켰습니다.
- Active project sessions에 global/per-project limit을 추가하고 새 session을 받기 전에 expired sessions를 prune해 stale entry가 무제한으로 쌓이지 않게 했습니다.

### Documentation

- source-copy 설치 안내를 명확히 해 사용자가 설치 가능한 addon 디렉터리만 복사하고 repository-level Companion, test, script development 디렉터리를 Godot 프로젝트에 넣지 않도록 했습니다.
- 네 언어 overview의 release-prep link를 업데이트해 v2.0.0 baseline이 v1.4.0 note가 아니라 v2.0.0 release-note source를 가리키도록 했습니다.
- localized docs에 v2 editor bridge upgrade contract를 추가해 bridge states, 명시적 editor-live upgrade requirements, project/session-scoped bridge use를 다룹니다.
- 네 개의 localized documentation tree에 v2.0.0 release-note source를 초기화해 feature work가 시작되기 전에 이 version line의 durable release narrative file을 마련했습니다.

### Internal

- v2 Companion .NET implementation을 Godot addon install surface 밖으로 옮기고 clean Asset Library install 및 project-discovery coverage를 확장해 plugin-owned Companion project가 user .NET project로 compile되거나 discover되지 않도록 했습니다.
- docs i18n validation에 각 overview page가 현재 `plugin.cfg` version의 release-note source를 link하도록 요구하는 coverage를 추가했습니다.
- v2 broker manifest, validator, negative policy tests, CI coverage를 추가해 broker lifecycle, transport, discovery, session-scope defaults가 explicit 및 non-persistent 상태로 유지되도록 했습니다.
- v2 broker manifest에 session lifecycle contract checks를 추가해 lease, renew/stop behavior, expired-session handling, active-session limits를 다룹니다.
- v2 bridge status schema, validator, negative policy tests, CI coverage를 추가해 online bridge status가 비어 있지 않은 editor session identity를 요구하도록 했습니다.
- matching online editor bridge가 있기 전에는 static/headless sessions가 editor-live capabilities를 선언하지 않도록 Companion contract coverage를 추가했습니다.
- static resource graph fallback diagnostics, preload/load classification, reparse-point references, bridge version policy samples, project session limits에 대한 regression coverage를 추가했습니다.
- .NET CI workflow에 Companion static analyzer build와 contract-runner coverage를 추가했습니다.
- Companion contract와 static analyzer build/test gate를 release workflows에도 반영하고, dry-run cache marker에 해당 check를 추가했으며, v2 contract workflows의 active v2 baseline push filters를 보호했습니다.
- branch policy와 version-policy validator의 허용 PR target에 `v2.0`을 추가하고, v2.0 release-baseline metadata change와 non-release version guardrail coverage를 보강했습니다.
- branch policy와 version-policy validator에서 명시적 v2 stacked PR base branch를 허용해, non-v2 target protection을 약화하지 않으면서 각 v2 Companion feature를 독립적으로 review할 수 있게 했습니다.

## [Unreleased] ([1.4.0])

대상 버전은 1.4.0입니다.

### Added

- `system_plugin_reload(action="full_reload_plugin")`와 maintenance reload path에 명시적인 self-plugin target metadata를 추가해 client가 MCP plugin 자체 reload와 다른 plugin operation을 구분할 수 있게 했습니다.
- MCP `tools/list` entry와 tool presentation node에 `outputSchema` metadata를 추가해 client가 input schema와 별도로 normalized tool result envelope를 확인할 수 있게 했습니다.
- `system_tool_catalog` search summary에 governance diagnostics를 추가했습니다. 사용 가능한 filter value, filter warning, domain / category filter가 없거나 너무 좁을 때의 suggested next query를 포함합니다.

### Changed

- built-in Prompt Guide argument handling을 강화해 `prompts/get`이 알 수 없는 argument name을 거부하고 요청된 prompt의 allowed argument list를 반환하도록 했습니다.
- `system_tool_activity`를 public MCP tool surface에서 제거했습니다. legacy call은 activity resources를 가리키는 `removed_public_tool` guidance를 반환합니다.

### Documentation

- README와 localized documentation entry page에 실제 프로젝트 검증 section을 추가하고 공개 Godot .NET project example link와 project screenshot을 넣었습니다.
- v1.4.0 release-note source를 초기화하고 localized release-note navigation과 validation map을 업데이트했습니다.
- `system_help` protocol guidance를 명확히 해 client가 MCP Resources는 수동 context, Prompts는 workflow planning, Tools는 action이나 계산된 workflow result에 쓰도록 구분할 수 있게 했습니다.
- [v1.4.0 리팩터 진행 추적 문서](프로세스/v1.4.0-리팩터-진행-추적.md)를 추가해 완료된 guardrail, 남은 cleanup axis, 다음 PR queue를 기록했습니다.

### Internal

- manifest 기반 public tool surface guardrail을 추가해 loader와 contract가 high-level MCP category만 공개하도록 했습니다.
- version-policy에 protocol facts JSON/fallback parity coverage를 추가하고 fallback error code default를 canonical facts file과 동기화했습니다.
- User tool runtime diagnostics에 recovery guidance field를 추가해 load failure가 diagnostic code, recommended action, follow-up tool hint를 포함하도록 하고, 관련 User tool과 runtime diagnostics contract를 plugin harness 필수 실행 대상에 포함했습니다.
- localized changelog section order를 검증하는 docs i18n validation coverage를 추가했습니다.
- tool list, catalog resource, search result, Tools page rendering이 `system_tool_activity`를 다시 노출하지 않도록 removal guard coverage를 추가했습니다.

## [1.3.0] - 2026-06-08

### Added

- v1.3.0 semantic editor automation model을 추가했습니다. editor UI guidance는 semantic workflow를 먼저 사용하고, control-level focus/text/value/popup action을 다음으로 사용하며, control-local mouse / pointer event는 fallback으로만 사용하도록 합니다.
- `system_editor_control(action="wait_for_ui")`를 추가했습니다. client는 automation workflow를 계속하기 전에 실제 editor control을 존재, 표시 상태, text, enabled / disabled state로 기다릴 수 있습니다.
- `system_editor_control(action="get_popup")`와 `system_editor_control(action="capture_popup")`를 추가했습니다. 반환된 popup path로 표시 중인 floating editor popup / window를 inspect / crop할 수 있습니다.
- `system_editor_control`과 low-level `ui_control` click action에 click fallback observation evidence를 추가했습니다. dispatch된 input event, Button-like target의 click 전후 state, 관측된 control signal, activation evidence가 없을 때의 hint를 반환합니다.
- `system_editor_evidence`를 추가했습니다. editor, control, popup, active-dialog, automatic surface를 위한 high-level visual evidence workflow이며 target, fallback, degradation, visible-popup, capture-policy metadata를 명시합니다.
- `system_settings_dialog`를 추가했습니다. Project Settings / Editor Settings에 대해 open/status/search/navigation, tab/category focus, row model, row resolution, value read/focus/set/verify, surface capture, close, trusted `run_task` orchestration을 제공합니다.
- `system_inspector`를 추가했습니다. Inspector visible property model, unique property resolution, typed value read, value focus, guarded text/number/bool write, verification, capture, current/prepared target용 trusted property task를 제공합니다.
- `system_project_lifecycle(action=start|stop)`을 표준 project runtime session lifecycle entry로 추가했습니다. marker validation, auto-stop, stop control, foreground-window fallback metadata, runtime capability context를 포함합니다.
- `system_tool_catalog`을 추가했습니다. client는 공개 tool을 query, category, domain으로 검색하고 match reason, action, parameter, visibility, enabled state, optional schema를 확인할 수 있습니다.
- `system_tool_activity` query filter와 slow/failure diagnostics를 추가했습니다. client는 state, tool, slow-call threshold로 running / recent call을 확인할 수 있습니다.
- User tool naming diagnostics를 추가했습니다. User tool listing, scaffold preview, compatibility report에서 declared name, normalized name, public MCP tool name을 표시합니다.
- `system_scene_inspect`를 추가했습니다. `validate`, `analyze`, `full` action을 가진 unified read-only scene inspection entry이며 기존 scene validation / analysis tool도 유지합니다.
- `system_plugin_maintenance`를 추가했습니다. status, reload, update-status, update-source selection, update-start workflow를 묶는 high-level plugin maintenance entry이며 전용 reload / update tool도 유지합니다.
- project configuration inspection을 추가해 export preset과 개별 input action을 읽을 수 있게 했습니다. sensitive export option key와 local absolute export path는 redact합니다.
- MCP resource와 resource template metadata를 지역화해 `resources/list`와 `resources/templates/list`가 hard-coded English 대신 현재 plugin language를 따르도록 했습니다.
- v1.4 canonical MCP resources를 추가해 guide discovery, project / editor state, activity status / recent read, exposed / visible tool catalog를 다루도록 했습니다. 기존 compatibility resource URI는 유지합니다.
- Config client card를 위한 안정적인 capability matrix를 추가해 support level, 사용 가능한 action metadata, note key를 포함했습니다.
- 모든 `tools/call` result에 `structuredContent`를 추가하고 기존 client용 text JSON content도 유지했습니다.

### Changed

- plugin metadata, protocol facts, .NET bridge metadata, plugin-update contract expectation, localized changelog, release-note source를 `1.3.0`으로 올렸습니다.
- `system_settings_dialog(action="capture")`를 변경해 visible settings popup / window bounds, dialog control bounds, full-editor screenshot 순서로 evidence를 가져오고 `capture_backend`와 `capture_target_path` metadata를 반환하도록 했습니다.
- project runtime control의 public surface를 `system_project_lifecycle(action=start|stop)`으로 변경하고 이전 project run/stop tool name의 compatibility alias를 제거했습니다.
- generated User-tool scaffold가 base tool script를 resource path로 상속하도록 변경해 headless catalog scan이 global script-class registration에 덜 의존하게 했습니다.
- structured tool result payload를 위해 protocol facts를 schema version `2026-06-08.27`로 업데이트했습니다.

### Fixed

- editor UI write action을 강화해 hidden, disabled, display-only, low-confidence settings control이 text/value mutation 전에 거부되도록 했습니다.
- filesystem / project-file tool과 .NET bridge의 project path handling을 강화해 remote tool request가 `user://`, absolute path, URI scheme, traversal segment를 통해 Godot project tree 밖으로 나가지 못하게 했습니다.
- release workflow를 수정해 기존 release tag가 target commit을 이미 가리키지만 GitHub Release 생성만 남은 경우에도 복구할 수 있게 했고, tag-triggered workflow는 release source만 검증하며 정식 Release를 만들지 않는다는 점을 명확히 했습니다.
- release verification이 Godot console executable을 안정적으로 선택하도록 수정하고, rendered commit summary가 설정된 commit limit을 지키도록 했습니다.
- Settings update source와 branch selector popup height를 제한해 긴 discovered ref list가 editor를 덮지 않고 menu 안에서 scroll되도록 했습니다.
- Tools page enabled / total count를 수정해 plugin-domain tool도 core / user tool domain과 함께 집계되도록 했습니다.
- MCP resource diagnostics redaction을 강화해 mixed-case URL credential, bearer / API-key variant, sensitive text marker, nested metadata secret이 output 보고 전에 마스킹되도록 했습니다.

### Documentation

- localized v1.4.0 protocol refactor plan을 추가하고 네 localized roadmap에서 link했습니다.
- README Highlights를 업데이트해 top-level feature story를 v1.2.0과 v1.3.0 release line에 맞췄습니다. 겹치는 entry를 editor-native service, semantic automation, evidence, diagnostics, runtime, workflow discovery, User-tool extension 같은 동일 계층 theme로 묶었습니다.
- v1.3.0 release documentation을 semantic editor automation과 evidence-backed workflow 중심의 설명으로 재구성했습니다.
- localized overview link와 release-note validation map을 업데이트해 English, Simplified Chinese, Japanese, Korean docs가 v1.3.0 release-note source를 가리키도록 했습니다.

### Internal

- localized v1.4.0 protocol plan page에 대한 documentation localization validation coverage를 추가했습니다.
- v1.4.0 contract case manifest를 추가하고 plugin harness required-case selection을 manifest label에서 파생하도록 했습니다.
- guarded write refusal path를 검증하는 editor UI와 Settings Dialog contract coverage를 추가했습니다.
- directory, file, JSON, search entry point에서 unsafe path rejection을 검증하는 filesystem executor contract coverage를 추가했습니다.
- v1.3.0 semantic-control priority model을 위해 editor UI prompt, help, localization, schema facts, tool tree, Tools page rendering, catalog discovery, harness contract를 업데이트했습니다.
- settings-dialog contract coverage를 확장해 `run_task` read/verify/set/set-and-verify flow, ambiguity / write-refusal guard, capture-required write preflight, tab/category/row/value action, prompt/help guidance, tool rendering을 검증했습니다.
- Inspector workflow contract를 추가해 property model resolution, typed read, guarded text/number/bool write, capture fallback, catalog discovery, prompt/help guidance, tool rendering을 검증했습니다.
- editor evidence surface contract를 추가해 exact/fallback/degraded capture behavior, visible-popup metadata, localized tool metadata, prompt/help guidance, Tools page rendering을 검증했습니다.
- project lifecycle contract를 추가해 explicit lifecycle action, marker validation, foreground-window fallback behavior, 이전 run/stop compatibility name 제거를 검증했습니다.
- tool catalog, tool activity, User-tool naming, scene inspect, plugin maintenance, project configuration, userdata maintenance contract coverage를 loader, router, Tools page, localization inventory, protocol facts 전반으로 확장했습니다.
- canonical guide, state, activity, tool-catalog resources와 activity-call resource template의 contract coverage를 추가하고 tool schema facts를 `2026-06-08.26`으로 올렸습니다.
- editor/system control harness coverage를 확장해 wait condition, popup capture, hover/leave pointer fallback, click input dispatch, Button-like target observation, activation-signal separation, input-only Button diagnostics를 검증했습니다.

## [1.2.0] - 2026-06-06

### Added

- MCP resources와 prompts에 output size safeguard를 추가했습니다. 과도하게 큰 file-backed resource는 전체를 읽기 전에 거부하고, 긴 prompt text는 `_meta` byte-size detail과 함께 잘라냅니다.
- `system_editor_control`에 top menu, popup selection, hover / leave, main-screen switching, distraction-free control action을 추가해 Agent가 OS mouse automation에 의존하지 않고 Godot editor UI를 더 넓게 다룰 수 있게 했습니다.
- `system_tool_activity`와 MCP tool call의 선택적 `_mcp_context` metadata를 추가했습니다. client는 개별 tool schema를 바꾸지 않고 실행 중인 call, 최근 완료 기록, 실행 순서, Agent 자체 보고 협업 context를 가볍게 볼 수 있습니다.
- `/health`에 HTTP client session identity와 request audit field를 추가했습니다. connection별 ID, request ID, client summary, active session, 최근 disconnected session을 포함해 multi-client diagnostics에 사용할 수 있습니다.
- `plugin_evolution_runtime_diagnostics`와 `project_state(include_runtime_health=true)`를 통해 User tool runtime diagnostics를 추가했습니다. discovery count, load failure, watcher state, compatibility summary, recent audit entry를 확인할 수 있습니다.
- Dock UI에서 선택할 수 있는 한국어 언어 옵션을 추가하고 Home, Tools, Config, Settings, tool preview, category, plugin developer surface의 주요 label을 한국어로 표시하도록 했습니다.
- Dock localization coverage와 validation을 확장해 한국어 UI와 기존 locale의 visible label, tool metadata, client configuration guidance, Prompt Guide text가 더 안정적으로 localized content를 사용하도록 했습니다. product name, path, identifier, technical abbreviation은 shared text로 유지합니다.
- health, plugin reload, plugin update response에 maintenance window contract를 추가해 client가 lifecycle reload나 update sync 중 temporary disconnect, reconnect requirement, tool-list refresh requirement, retry guidance를 판단할 수 있게 했습니다.
- maintenance window response의 reconnect guidance를 지원되는 모든 Dock language로 localization했습니다.
- `system_editor_plugin_control`을 추가해 client가 third-party EditorPlugin의 project-setting state, editor-session state, visible UI hint, restart/manual-activation guidance, guarded enable/disable behavior를 확인할 수 있게 했습니다.

### Changed

- Plugin update sync는 addon file을 쓴 뒤 Godot editor file-system scan을 refresh하고 완료를 기다린 다음 plugin lifecycle reload를 schedule하도록 바뀌었습니다.
- Public documentation entry point를 localized documentation tree로 옮기고 locale별 directory name과 file name을 사용하도록 바꿨습니다.
- Release rendering은 영어 `v1.2.0` release note에서 GitHub Release body를 생성하도록 바뀌었고, 저장되는 release notes는 영어, 간체 중국어, 일본어, 한국어판으로 link합니다.

### Fixed

- malformed JSON-RPC parameter handling을 수정했습니다. HTTP와 stdio request는 object가 아닌 top-level `params`를 일관되게 `-32602`로 거부하고, object가 아닌 `tools/call.arguments`는 tool-level `isError` result를 유지합니다.
- C# empty-method generation을 수정해 body가 없을 때 internal edit helper가 모호한 fallback body를 만들지 않도록 했습니다.
- plugin evolution runtime diagnostics를 수정해 공개 `runtime_diagnostics` entry가 live User-tool runtime snapshot을 forward하고 project health diagnostics와 맞게 했습니다.
- HTTP transport pipelining을 수정했습니다. keep-alive connection에 이미 buffer된 여러 request가 async route 완료 뒤에도 계속 drain되어 추가 socket byte를 기다리지 않습니다.
- multi-client reconnect recovery를 위해 HTTP reconnect backpressure safeguard를 추가했습니다. 한 frame에서 여러 pending connection을 받을 수 있고, 과도한 pending request buffer를 거부하며, response write failure 뒤 client connection을 닫습니다.
- stdio frame processing에 async reentry guard, stop/restart response write guard, one-frame request burst handling을 추가하면서 tool-loader ticking을 유지해 연속 stdio request 안정성을 높였습니다.
- lifecycle reload scheduling failure를 수정해 실패한 schedule attempt가 plugin reload state를 pending으로 남기지 않도록 했습니다.
- project file reimport handling을 수정해 `project.godot`, text file, sidecar metadata, 지원하지 않는 path는 Godot import pipeline으로 넘기지 않고 structured `not_importable_resource` error를 반환하게 했습니다.
- popup menu automation guardrail을 수정해 hidden popup, disabled item, separator, submenu row, duplicate text, conflicting selector는 잘못된 entry를 선택하지 않고 명확히 실패합니다.
- DAP debugger size / timeout guardrail을 수정해 비정상 endpoint가 긴 Agent request를 점유하지 않고 명확한 `dap_limit_exceeded` error를 반환합니다.
- localized documentation의 facts, links, encoding examples, release-note wording, UI / Config references, obsolete draft fragments, 일본어 README의 invalid screenshot reference를 수정했습니다.

### Documentation

- `docs/en/ROADMAP.md`와 `docs/zh-CN/路线图.md`에 영어와 간체 중국어 roadmap entry point를 추가했습니다.
- `docs/` 아래에 영어, 간체 중국어, 일본어, 한국어 전체 documentation tree를 추가하고 각 locale이 localized directory name과 file name을 사용하도록 했습니다.
- Public docs navigation을 localized documentation tree로 옮기고 오래된 root technical-structure documentation page를 public docs set에서 제거했습니다.
- README와 changelog entry를 localized documentation tree로 옮긴 뒤 root-level redundant localized README와 changelog copy를 제거했습니다.
- 영어, 간체 중국어, 일본어, 한국어의 persistent `v1.2.0` release notes를 마무리하고 title과 introduction 바로 아래에 cross-language release-note links를 추가했습니다.
- Release runbook과 release-note renderer documentation을 업데이트해 formal GitHub Release가 영어 release note와 영어 changelog를 rendered body source로 쓰도록 기록했습니다.

### Internal

- JSON-RPC request, HTTP server, stdio resources/prompts, tool router contract를 확장해 `2026-06-05.6` tool schema facts update와 함께 malformed `params`와 `tools/call.arguments` boundary를 검증합니다.
- `mcp_resources_prompts_contracts`를 확장해 resource output limit, prompt truncation metadata, `2026-06-05.5` tool schema facts update를 검증합니다.
- script edit service harness coverage를 확장해 C# empty method body에 모호한 generated fallback body 대신 명시적인 `NotImplementedException` guard body를 요구합니다.
- User-tool service, runtime health, project-state harness coverage를 확장해 runtime diagnostics와 failed custom-tool load reporting을 검증합니다.
- plugin-evolution harness coverage를 확장해 공개 runtime diagnostics가 live User-tool runtime snapshot을 계속 forward하는지 검증합니다.
- tool schema facts version을 올리고 menu control, popup selection, hover / leave, main-screen switching, distraction-free action에 대한 editor UI harness coverage를 확장했습니다.
- tool activity registry coverage를 router, stdio, registry, tool-loader harness contract에 추가해 context stripping, response activity summary, transport coverage, 공개 `system_tool_activity` entry를 검증합니다.
- Tools page documentation을 업데이트해 `system_tool_activity` tree entry와 `_mcp_context` protocol boundary를 추가했습니다.
- 공개 `system_tool_activity` surface에 맞춰 protocol facts의 tool schema version을 업데이트했습니다.
- docs i18n validation을 추가하고 확장해 tree-shape hash, localized path mapping, Markdown link, draft wording, cross-locale path leakage, redundant root docs, README presentation asset, duplicate path key를 검증합니다.
- plugin metadata, protocol facts, .NET bridge metadata, plugin-update contract fixture expectation을 `1.2.0` development line으로 전환했습니다.
- locale contract coverage를 업데이트해 full English table을 복제하지 않고 localized override를 유지하는 language도 merged fallback translation으로 검증할 수 있게 했습니다.
- localization CI contract를 강화해 모든 supported locale이 같은 key set을 공개하고, 명시적으로 shared인 product name, path, identifier, technical abbreviation 외에는 English string을 조용히 재사용할 수 없게 했습니다.
- refactor guardrail scan을 수정해 banned-source identifier audit가 자기 validation script를 violation으로 보고하지 않도록 했습니다.
- HTTP transport, health, plugin reload, plugin update, maintenance-contract harness coverage를 확장해 reconnect-aware client, pipelined request, localized reconnect hint, stale-schema precedence, stable connection ID, request audit ID, recent disconnected-session reporting을 검증합니다.
- headless HTTP server runtime-control setup을 보호해 standalone harness server가 plugin이 아닌 parent를 runtime-control service에 전달하지 않도록 했습니다.
- plugin harness failure report를 확장해 `failureClasses`, `primaryFailureClass`, `exitCleanupWarningFailure`로 Godot exit cleanup warning이 runtime error marker에 가려지지 않게 했습니다.
- release workflows를 업데이트해 네 가지 localized changelog와 release-note source를 검증하면서 GitHub Release body를 영어 source file에서 render하도록 했습니다.

## [1.1.2] - 2026-06-02

### Changed

- 내장 MCP Prompt Guide를 여섯 개의 작업 흐름 진입점으로 재정리했습니다. `godot.project_orientation`, `godot.content_authoring`, `godot.debug_triage`, `godot.reference_integrity`, `godot.runtime_validation`, `godot.editor_ui_control`입니다.
- 디버거 가이드는 `godot.debug_triage`로 모아, prompt 탐색 결과가 독립 디버거 안내가 아니라 실패 진단 흐름으로 보이게 했습니다.

### Fixed

- `system_help`에서 재구성된 MCP Prompt Guide를 노출해, Agent가 주요 능력 설명에서 `prompts/list`, `prompts/get`, 여섯 개 내장 prompt ID를 찾을 수 있게 했습니다.
- DAP 디버거의 Tools 페이지 분류, 동작 이름, 매개변수 설명의 현지화를 보완해, 로컬라이즈된 도구 미리보기에서 원래 영문 schema로 되돌아가지 않게 했습니다.
- Tools 페이지의 동적 동작과 빈 매개변수 fallback 문구를 현지화하고, 특정 도구 key가 없을 때는 기존 schema 설명을 유지하도록 했습니다.
- 깨끗한 Asset Library 설치를 수정했습니다. 내보낸 플러그인 다운로드에 Roslyn bridge 구현 소스가 더 이상 포함되지 않아, 호스트 Godot C# 프로젝트가 이 소스를 직접 컴파일하지 않게 됩니다.
- 프랑스어 현지화 파일에서 악센트, 굽은 따옴표, 줄바꿈 없는 공백, 합자가 깨지지 않도록 수정했습니다.
- reference-integrity Prompt Guide의 `resource_path` 매개변수와 system_resource_reference_audit의 텍스트 파일 지원 범위를 `.tscn`, `.tres`로 맞췄습니다.

### Documentation

- 이번 Prompt Guide, 현지화, 깨끗한 Asset Library 설치 유지 버전을 위한 `v1.1.2` 수기 릴리스 노트 원본을 추가했습니다.
- 플러그인 패키지 안의 README 복사본을 갱신해, Asset Library 설치 후 내보낸 패키지가 저장소 문서, 변경 로그, 현재 `dev` 분기 미리보기 이미지를 가리키도록 했고, 패키지 내부의 내보내지 않은 상대 경로는 더 이상 가리키지 않게 했습니다.
- Prompt Guide 문서를 갱신해 여섯 개의 상위 작업 흐름 진입점을 설명하고, DAP 디버깅은 `godot.debug_triage`의 진단 분기이지 독립 prompt guide가 아님을 분명히 했습니다.

### Internal

- 실제 tool-loader를 기반으로 한 현지화 inventory 계약을 추가해, 지원 언어 전체의 Tools 페이지 도구 트리, 동작, 매개변수 fallback 문구를 검증하도록 했습니다.
- 지원 언어 중 하나라도 다른 locale에 이미 있는 번역 key를 놓치면 CI가 멈추는 key parity harness 계약을 추가했습니다.
- MCP prompt, system help, router, 현지화 계약을 갱신해 prompt surface가 여전히 여섯 개 상위 작업 흐름 가이드로 유지되도록 했습니다.

## [1.1.1] - 2026-05-31

### Added

- `system_dap_debugger`를 완전한 Debug Adapter Protocol 세션 진입점으로 확장했습니다. 런타임 설정, 지속 session ID, `initialize`, `launch`, `attach`, `configuration_done`, `threads`, `terminate`, `disconnect`를 지원하면서 단일 고수준 DAP 도구 표면은 그대로 유지합니다.

### Changed

- 내장 MCP Prompt Guide를 강화했습니다. `godot.scene_bootstrap`, `godot.debug_triage`, `godot.binding_fix`는 이제 추천 도구 순서, 검증 요구, 피해야 할 항목을 포함한 실행 가능한 작업 흐름 설명을 돌려줍니다.

### Fixed

- `system_bindings_audit`가 대형 프로젝트에서 편집기를 멈출 수 있던 문제를 고쳤습니다. 같은 호출 안에서 장면 감사 결과를 캐시해 각 고유 장면을 한 번만 로드하고 인스턴스화하며, 연속된 원자 호출에서 executor를 재사용해 reference index와 Roslyn cache를 스크립트 검사 사이에 유지합니다.
- atomic executor cache 무효화 판단을 고쳤습니다. `get_settings` 같은 읽기 작업이 부분 문자열로 인해 쓰기 작업으로 오판되지 않으며, 쓰기 성공 후에는 cached executor를 지워 오래된 reference와 Roslyn 데이터를 막습니다.
- `system_project_state(summary=true)`와 summary 분기만 요청하는 경우에도 완전한 script, scene, resource 경로 배열을 먼저 만들던 문제를 고쳤습니다. 이제 한 번의 batch 경량 파일 계수 뒤에 간결한 payload를 돌려줍니다.
- `system_help`로 MCP Prompt Guide를 노출해, Agent가 주요 능력 설명에서 `prompts/list`, `prompts/get`, 세 개의 내장 prompt ID를 찾을 수 있게 했습니다.

### Documentation

- `system_project_state(sections=[...])`가 `summary=true`보다 우선하며, `health` 섹션은 플러그인 건강 수집을 유발하고, 대형 프로젝트에서 완전한 경로 배열이 필요한 경계는 `files` 섹션이라는 점을 기록했습니다.
- Prompt Guide의 발견과 사용 방식, 그리고 MCP prompt와 실행 가능한 tool의 경계를 도구 시스템, 서비스 라우팅, 런타임 서비스 문서에 기록했습니다.

### Internal

- 지속 fake server 생명주기 흐름, 기본적으로 loopback만 허용하는 보안 경계, 원시 응답 비식별화를 포함하도록 DAP 계약 harness를 확장했고, `dap_invalid_session_state` / `dap_invalid_settings` / `dap_limit_exceeded` 프로토콜 오류 표식을 공개했습니다.
- `atomic_bridge.call_atomic` / `call_atomic_async`에서 atomic executor 인스턴스를 재사용하도록 바꿔, 연속된 atomic 호출이 `reference_service._reference_index`, `inspect_service._plugin_roslyn_service`, 그 밖의 인스턴스 캐시를 유지하게 했습니다.
- `system_project_state`의 간결한 읽기가 완전한 경로 열거를 건너뛰고, count-only 요청 한 번으로 프로젝트 파일 총수를 집계하며, 기본 전체 payload와 `files` 섹션은 여전히 필요할 때 파일 경로를 수집한다는 점을 검증하는 계약을 늘렸습니다.
- prompt guide, system help, router, 현지화 harness 계약을 확장해 prompt 깊이, prompt 발견성, 실제 prompt ID, 다국어 Help 설명이 다시 퇴보하지 않도록 했습니다.

## [1.1.0] - 2026-05-28

### Added

- `system_dap_debugger`와 내부 `dap` 도구 분류를 추가했습니다. Godot Debug Adapter Protocol의 endpoint 상태, 중단점 설정과 제거와 목록, 일시 중지와 계속과 단일 단계 건너뛰기, 호출 스택과 출력 이벤트 읽기를 지원하며, `Content-Length` JSON frame 형식을 사용하고 `dap_unavailable` / `dap_response_failed` 오류 표식을 공개합니다.
- 1급 MCP Resources와 Prompts 지원을 추가했습니다. 클라이언트는 프로젝트 정보와 진단 요약 resource를 찾을 수 있고, 엄격한 `res://` 템플릿으로 scene, script, resource 파일을 읽을 수 있으며, `godot.scene_bootstrap`, `godot.debug_triage`, `godot.binding_fix` guide prompt를 받을 수 있습니다.
- `system_project_state(summary=true)`의 간결한 요약과 `system_project_state(sections=[...])`의 분할 읽기를 추가했습니다. 대형 프로젝트는 핵심 건강, 파일, 런타임, 능력 비트, 플러그인 건강 구간을 필요할 때만 읽을 수 있습니다.

### Fixed

- 플러그인 시작과 설정 영속화 경로를 고쳤습니다. 런타임 상태, 설정 저장소, 핵심 서비스가 먼저 초기화된 뒤 load/save나 갱신 콜백이 접근하게 했습니다.
- 플러그인 harness 검증을 고쳤습니다. Godot stdout/stderr에 `SCRIPT ERROR:`, `Invalid call.`, `Parse Error:` 같은 런타임 또는 파싱 오류 표식이 나타나면, 프로세스 종료 코드가 성공이어도 바로 실패로 처리합니다.

### Documentation

- 이 버전의 디버깅, resources, project state, 시작 검증을 위한 `v1.1.0` 수기 릴리스 노트 원본을 추가했습니다.
- README, 런타임 서비스, 도구 시스템, Tools 페이지, 테스트, CI 문서를 갱신해 MCP Resources와 Prompts, DAP 디버거 도구, `system_project_state`의 간결한 읽기, tool catalog resources, harness 검증 동작을 다뤘습니다.
- 영문, 중국어, 독일어, 스페인어, 프랑스어, 일본어, 포르투갈어, 러시아어 locale resource의 DAP와 system 도구 설명을 보완했고, 릴리스 Runbook에 emoji 릴리스 노트 템플릿과 Documentation / Internal changelog 유지 규칙을 기록했습니다.

### Internal

- 플러그인 harness와 계약 테스트를 확장해 DAP 디버거 흐름, MCP Resources와 Prompts 라우팅, `system_project_state`의 간결한 읽기, JSON-RPC resource/prompt 방법, tool-loader catalog 분류, debug executor 호환성, 업데이트 fixture, 플러그인 진입점 초기화를 다뤘습니다.
- Godot harness에 런타임과 파싱 오류 표식 감지를 추가해, Godot 프로세스가 성공 종료되어도 stdout/stderr 진단이 실패로 이어지게 했습니다.
- Tools 페이지 렌더링 harness case를 분리하고, 플러그인 쪽 Roslyn harness 경로를 갱신해 새 프로토콜과 도구 표면이 확장되어도 required validation이 안정적으로 유지되게 했습니다.

## [1.0.1] - 2026-05-26

### Fixed

- `system_resource_reference_audit`를 고쳤습니다. 이제 Roslyn `types[]` 메타데이터로 유효한 C# `[GlobalClass] Resource` 스크립트를 해석해 class unresolved를 잘못 보고하지 않으며, 빠진 id 진단을 내리기 전에 따옴표 없는 `ExtResource id=` 선언을 식별하고, resource id를 파싱할 때 따옴표 있는 속성값 내부의 `id=` 텍스트는 무시합니다.
- Tools 페이지 preview 패널을 고쳐, 선택한 항목 설명이 아래 분할 영역을 가득 채우고 아래쪽 빈 공간이 남지 않게 했습니다.

### Documentation

- 이번 안정선 유지 업데이트를 위한 `v1.0.1` 수기 릴리스 노트 원본을 추가했고, 플러그인 메타데이터는 이미 `1.0.1`로 전환되었으므로 오래된 `v1.0.0` 원본은 제거했습니다.
- 수기 릴리스 노트의 문체와 템플릿 제약을 보강해, 설명이 사용자 중심을 유지하고 `v1.0.0-pre3`의 서사 구조를 따른 채, 배포 기계 변화만 따로 쓰지 않도록 했습니다.
- 릴리스 변경 기록을 정리해 `v1.0.1` 구간이 `v1.0.0` 이후 변경만 반영하도록 했습니다.

### Internal

- 기본적으로 dry-run 먼저 하는 원클릭 릴리스 workflow를 추가했습니다. 새 `v*` GitHub Release를 만들기 전에 `dev` 출처, 버전 메타데이터, 수기 릴리스 노트, 중복 tag와 release, 빌드 산출물, 플러그인 harness를 검사하고, 성공한 dry-run을 기록해 같은 커밋의 정식 릴리스는 build와 harness 검사를 다시 하지 않아도 되게 했습니다. tag 트리거 릴리스는 먼저 tag가 `dev`에서 도달 가능한지 확인한 뒤 읽기 전용 권한을 유지합니다.
- 원클릭 릴리스 workflow의 수동 트리거 인터페이스를 단순화해, 출시 소스는 GitHub Actions의 `Use workflow from` 분기 선택기만 쓰게 했습니다.
- 플러그인 메타데이터, 프로토콜 사실 파일, .NET bridge 메타데이터를 `1.0.1` 안정 유지 버전으로 바꿨습니다.
- 헤드리스 검증은 Godot console 실행 파일을 쓰도록 바꾸고, GUI 실행 파일은 더 이상 쓰지 않게 했습니다.
- 신뢰 가능한 PR 버전 전략 workflow를 추가해, 릴리스 마감 전에 릴리스 분기가 아닌 곳에서 공개 버전 메타데이터를 바꾸지 못하게 했습니다.
- plugin harness required subset 실행 시간을 줄였습니다. 일반 headless case는 하나의 stage와 Godot 실행 안으로 합치고, editor probe case는 분리했으며, 콘솔과 GitHub Step Summary에 stage와 case별 시간을 출력합니다.

## [1.0.0] - 2026-05-26

### Changed

- Dock의 영속 설정 컨트롤을 새 Settings 페이지로 분리해, Home 페이지가 진단, 서비스 상태, 빠른 서비스 동작에 집중하도록 했습니다.
- Settings 업데이트 방식에 분기 선택, 최신 안정판, 최신 릴리스판, 지정 릴리스와 태그를 추가했고, GitHub reference discovery 드롭다운을 통해 선택할 수 있게 했습니다.
- 플러그인 내부 안전 업데이트 동기화를 추가했습니다. GitHub archive에서 `addons/godot_dotnet_mcp/`만 추출하고 `custom_tools/`는 유지하며, 동기화 메타데이터를 쓰고, 선택한 업데이트 방식에 따라 자동으로 reference를 찾고, 최신 릴리스 대상은 GitHub Releases에서만 가져오고, 동기화 동작이 선택한 대상을 처리합니다.
- `system_plugin_update`를 추가해, MCP 클라이언트가 설치된 플러그인 버전과 지문을 읽고, 업데이트 출처를 고르며, 비동기 reference discovery나 sync를 시작하고, sync와 reload 진행 상황을 폴링할 수 있게 했습니다.
- Settings 동기화가 성공하면 지연된 플러그인 생명주기 재시작을 예약해 갱신된 플러그인 파일이 바로 반영되게 했습니다.
- Settings 업데이트 페이지에서 중복되던 현재 버전, 플러그인 경로, commit summary 줄을 제거했습니다.
- 편집기 Dock 탭과 제목 문구를 조정해, 탭에는 `MCP`, Dock 제목과 팝업 제목에는 `Godot .NET MCP`가 보이게 했습니다.

### Fixed

- 설정 페이지의 클라이언트 동작 버튼이 처음 그려질 때 레이아웃 너비가 아직 준비되지 않아 한 줄짜리 단일 열로 보이다가 클라이언트를 바꿔야만 돌아오던 문제를 고쳤습니다.
- debug `dotnet` 기본 C# 프로젝트 발견을 고쳤습니다. 자동 build / restore 선택 시 플러그인 bridge 프로젝트를 건너뛰어 사용자 프로젝트로 착각하지 않게 했습니다.

### Documentation

- 루트 README의 제품 페이지를 갱신해 새 홍보 이미지를 넣고, 한국어와 영어 문구를 맞추고, 배지 수를 줄였습니다.
- README의 배지 문구를 갱신해 정식판과 사전 릴리스 진입점을 제품 페이지에서 더 명확하게 보이게 했습니다.
- README와 릴리스 노트에, 그래픽 UI 파일 업데이트나 MCP 프로젝트 파일 도구를 통해 소스 복사로 설치한 플러그인을 최신 GitHub 코드와 맞출 수 있다는 설명을 추가했습니다.
- `v1.0.0` 수기 릴리스 노트 원본을 새로 만들고, 정식판 배포 흐름 문서도 함께 갱신했습니다.
- `v1.0.0` 수기 릴리스 노트를 더 완전한 최초 안정판 개요로 확장했고, pre3의 배포 서사 스타일을 유지했습니다.
- 릴리스 변경 기록을 정리해 `v1.0.0` 구간이 `v1.0.0-pre3` 이후 개발 내용만 반영하고, 이미 배포된 사전 릴리스 기록은 섞지 않게 했습니다.

### Internal

- 기본적으로 dry-run 먼저 하는 원클릭 릴리스 workflow를 추가했습니다. 새 `plugin-v*` GitHub Release를 만들기 전에 `dev` 출처, 버전 메타데이터, 수기 릴리스 노트, 중복 tag와 release, 빌드 산출물, 플러그인 harness를 검사합니다.
- PR policy 검사를 실시간 PR 메타데이터를 읽도록 바꾸고, 수동 dispatch 대체 경로를 추가해 PR 본문을 편집한 뒤에도 오래된 rerun payload에 의존하지 않고 다시 검사할 수 있게 했습니다.
- 플러그인 메타데이터, 프로토콜 사실 파일, .NET bridge 메타데이터를 `1.0.0` 정식 버전으로 바꿨습니다.
- 등록되지 않은 옛 플러그인 집계 도구 executor와 오래된 문서 참조를 제거했고, 분리된 플러그인 도구 분류 계약 검증을 강화했습니다.
- 공개 문서, issue 템플릿, harness fixture의 저장소 내부 프로젝트 이름을 플러그인 범위 표현과 중립적인 예시 경로로 바꿨습니다.
- release notes commit summary가 이전 릴리스 tag 경계를 꼭 해석하도록 하고, 더 이상 임의의 최근 commit 목록으로 되돌아가지 않게 했습니다.

## [1.0.0-pre3] - 2026-05-21

### Added

- `system_project_run`에 선택적 runtime bridge 로그 marker 검증을 추가했습니다. success / failure marker 일치, timeout 처리, marker 모드 자동 중지, fake runtime event를 통한 계약 검증을 지원합니다.
- 런타임 전면 창 능력 보고를 추가했고, background / minimized / no_focus 실행을 지원하지 않을 때는 `requires_foreground_window` 구조화 거절 정보를 돌려줍니다.
- Tools 페이지 팝업 좌표 의미의 계약 검증을 추가했고, 실제 오른쪽 클릭 경로와 Dock 오버레이 위치 계산에 쓰는 local / canvas / viewport / screen 좌표 경계를 분명히 했습니다.

### Fixed

- 플러그인 자체 진단의 느린 작업 보고를 고쳐, 시작과 재시작 중 가장 느린 단계를 표시하고, 진단 텍스트 복사본에 단계별 소요 시간을 넣게 했습니다.
- Config 페이지 클라이언트 카드 능력 설명을 고쳐, 완전한 원클릭 설정, CLI 원클릭 추가, 열기와 경로 관리만 지원하는 클라이언트, 수동 연결 안내형 클라이언트를 명확히 구분하게 했습니다.
- 빠른 .NET build와 플러그인 harness build 실패 진단을 고쳤습니다. Godot `.godot/mono/temp` 산출물이 `CS2012` 파일 잠금 오류를 만들면 `transient_file_lock`으로 분류하고 복구 방법을 보여줍니다.
- MCP 서버 수신 실패 자체 진단을 고쳤습니다. 포트 점유, 바인딩 거부, Windows 예약 또는 제외 TCP 포트는 서로 다른 원인과 처리 힌트를 보여줍니다.
- 런타임 스크린샷을 고쳤습니다. headless 또는 dummy 렌더러에서는 구조화된 skipped 결과를 돌려주고, 사용할 수 없는 viewport 스크린샷을 더 이상 시도하지 않습니다.
- 런타임 디버그 브리지 메시지 형식을 고쳐, 프로젝트 시작 시 runtime event / log / reply를 보낼 때 Godot 출력에 `Invalid message received` 오류가 나오지 않게 했습니다.
- `system_project_run` marker 검증이 live shared runtime bridge event를 읽어, 새 실행 event와 실행 전 marker 텍스트가 같아도 필터링하지 않게 했고, event-id cursor로 marker event를 소비해 로그량이 많아도 최신 tail window에 밀려나지 않게 했습니다. 합쳐진 live / fallback event cursor도 fallback event 삽입과 완전한 buffer 자르기 후 순서를 유지하게 했습니다. tail batch가 가득 찼을 때는 poll을 양보합니다.
- 도구 context 보조 함수를 고쳐, editor interface를 덮어쓴 객체로 도구를 실행할 때 Godot GDScript VM 내부 오류가 나지 않게 했습니다.
- `system_project_run` 실패 진단을 고쳐, 프로젝트 시작 중 `Editor interface not available`이 불일치로 나타나면 state probe와 run invoker 비교, 복구 조치, 경로 정보가 충분할 때 CLI fallback을 보여주게 했습니다.
- `system_project_state`와 `system_resource_reference_audit`의 프로젝트 파일 열거를 고쳐, 빈 스캔이 이제 의심 진단을 내리고 자산 감사 clean으로 잘못 판정되지 않게 했습니다.
- TileMap 도구 스크립트 파싱 문제를 고쳐, TileMap 도구 도메인이 MCP 도구 등록 중 정상적으로 인스턴스화되게 했습니다.

### Documentation

- `v1.0.0-pre3` 수기 릴리스 노트 원본을 추가해, 두 층 구조의 GitHub Release 본문을 만들 수 있게 했습니다.
- 오래된 `v1.0.0-pre2` 수기 릴리스 노트 원본을 제거해, 릴리스 분기가 현재 pre3 원본만 남기도록 했습니다.
- Tools 페이지 팝업 좌표 경계, 편집기 제어 책임, 런타임 전면 창 제한, no-focus 능력 필드, 실행 로그 marker 검증 설명을 보강했습니다.
- 릴리스 노트 원본, draft 미리보기, 정식 릴리스 렌더링 흐름 문서를 보강했습니다.
- CI와 테스트 문서를 갱신해 harness 소요 시간 요약, 실패 진단 artifact, 캐시 동작, 관리형 .NET SDK 선택, PR 검증 트리거, relay의 PR 생성 policy 경로를 다뤘습니다.
- PR, Issue, 릴리스, Agent 흐름 문서를 새로 만들거나 다듬어 단기 브랜치 기여 흐름과 맞췄습니다.
- PR 템플릿을 Summary / Changes / Screenshots / Testing / Related Issues로 단순화했고, 자세한 준비 규칙은 흐름 문서에 남겼습니다.
- 공개되지 않은 변경 기록을 정리해 pre3 구간이 현재 개발 이력을 반영하고 오래되거나 어긋난 항목은 제거했습니다.

### Internal

- CI workflow를 바꿔, 관리형 Windows runner에 미리 설치된 .NET 8 SDK를 쓰고 `global.json`으로 SDK 선택을 제한했습니다.
- CI의 push 트리거를 `dev`로 좁히면서 PR, merge queue, 수동 검증 진입점은 유지해, 같은 분기 PR의 중복 실행을 줄였고 required check 이름은 바꾸지 않았습니다.
- 빠른 .NET build와 무거운 plugin harness workflow에 PR 전용 concurrent cancel과 job timeout을 추가해, PR이 아닐 때의 동작은 바꾸지 않았습니다.
- 무거운 plugin harness 스크립트에 timing 출력과 선택적 GitHub Step Summary 요약을 추가해 느린 case나 단계를 찾기 쉽게 했습니다.
- CI에서 plugin harness 실패 진단을 유지하고 업로드하면서, 성공 실행 뒤 정리 동작도 유지했습니다.
- build workflow에 NuGet cache를 추가했고, plugin harness가 쓰는 Godot 4.6 mono 압축 해제 디렉터리도 cache했으며, 기존 check 이름은 유지했습니다.
- 가벼운 PR policy 검사를 추가해, 객관적인 PR 제목, 요약, 변경 내용, 테스트 설명 필드를 다루게 했습니다.
- actions-bot relay가 만드는 PR 본문에 base/head SHA, 변경 경로, diffstat, 트리거 사용자, run URL, 검증 workflow 링크 메타데이터를 추가했습니다.
- `actions-bot-relay` workflow를 추가해 `github-actions[bot]`이 patch 기반 단기 브랜치 PR을 제출할 수 있게 했습니다.
- PR 대상 분기 전략과 빠른 .NET build 검사를 무거운 Godot harness에서 분리하면서도 `validate-plugin-harness` check 이름은 유지했습니다.
- release note 렌더링 스크립트를 추가해 `next` draft release와 정식 tag release가 같은 "수기 요약 + 자동 commit summary" 본문을 쓰게 하고, 대응하는 changelog 구간도 검증하게 했습니다.
- 릴리스 자동화를 갱신해 검증을 수행하고 GitHub Release를 만들되, zip 패키지 자산은 만들지 않게 했습니다.

## [1.0.0-pre2] - 2026-05-06

### Added

- `system_editor_control`에 컨트롤의 로컬 왼쪽 클릭과 오른쪽 클릭, 팝업 메타데이터, 다중 좌표계 매핑 지원을 추가해, Agent가 편집기 UI를 더 믿을 만하게 찾고 조작할 수 있게 했습니다.
- `system_project_state`와 `system_editor_state`에 더 분명한 런타임 능력 보고를 넣었고, `system_project_run` 실패 맥락도 강화해 프로젝트 시작, 런타임 제어, 스크린샷 능력이 준비됐는지 판단하기 쉬워졌습니다.
- `system_plugin_reload(action="full_reload_plugin")`, 건강 상태 점검, Tools 페이지 현지화 설명을 추가해, Agent가 플러그인을 다시 불러오고 현재 실행 인스턴스가 설치된 파일과 일치하는지 확인할 수 있게 했습니다.
- `/health`, `system_editor_state`, `system_project_state`에 편집기 세션 식별자를 추가해, Agent가 현재 MCP 편집기 세션과 다른 Godot 프로세스를 구분하기 쉽게 했습니다.
- `system_resource_reference_audit`를 추가했고, `system_scene_validate`의 UID / fallback path 힌트도 강화해, `.tscn` / `.tres`의 오래된 참조와 C# custom Resource 스크립트 불일치 같은 `dotnet build` 이후에도 남을 수 있는 로드 위험을 찾게 했습니다.

### Changed

- 런타임 스크린샷과 입력 진입점을 `system_runtime_step(action=step|capture|input)`으로 합쳐, 공개 런타임 자동화 도구는 높은 수준의 입구를 유지하면서 도구 트리에는 내부 atomic tool 연결을 남겼습니다.

### Fixed

- `custom_tools/`에서 불러온 User 도구가 MCP `tools/list`에 들어가지 않던 문제를 고쳐, 이제 클라이언트가 Tools 페이지 상태에서만이 아니라 직접 이 도구를 찾고 호출할 수 있게 했습니다.
- 플러그인이 보이는 MCP server 포트를 고쳐, Settings에서 `3001` 같은 비기본 포트를 지정하면 여러 Godot 편집기 세션에서도 그 설정이 유지되고 상속된 서비스 환경 변수에 덮이지 않게 했습니다.
- Config 페이지 코드 블록 복사 버튼을 고쳐, 생성된 설정 내용 위에 마우스를 올리면 버튼이 계속 보이고 주기적 UI 갱신 때문에 숨겨지거나 복사 동작이 사라지지 않게 했습니다.
- Tools 페이지 도구 트리 언어 갱신을 고쳐, Dock 언어를 바꾸면 도구, 내부 노드, action 레이블이 바로 갱신되고 플러그인을 완전히 다시 시작하지 않아도 되게 했습니다.
- `system_script_patch` / `edit_gd add_variable`의 GDScript 변수 기본값 처리를 고쳐, `default_value`가 스크립트 파일에 제대로 기록되고 `system_script_analyze`가 이를 세고 보고할 수 있게 했습니다.
- 완전 플러그인 재시작 뒤 도구 갱신 문제를 고쳐, 다시 연결하면 새 System 도구와 schema 변화를 바로 볼 수 있게 했습니다.
- 로컬 HTTP 서비스의 CORS 처리도 고쳐, 기본적으로 임의의 출처에 교차 출처 접근을 더 이상 열지 않지만 이미 설정된 브라우저 클라이언트는 출처 검사로 계속 쓸 수 있게 했습니다.
- 리소스 참조 감사를 고쳐, UID 대상과 fallback path가 둘 다 없을 때 올바르게 오류를 보고하고, 일반 `.tscn` C# 노드 스크립트를 custom Resource 스크립트로 잘못 판단하지 않게 했습니다.

## [1.0.0-pre1] - 2026-04-28

### Added

- Roslyn 기반 내부 .NET Bridge 지원 라이브러리를 추가해, 초기 C# 작업 흐름을 C# 진단, C# 파일 읽기와 패치 수정, `.csproj` 읽기와 쓰기, 솔루션과 프로젝트 정보 검사로 확장했습니다.
- `system_help`를 추가해, Agent가 연결한 뒤 플러그인 능력, 추천 시작 단계, 스크린샷 우선 팁, 숨겨진 컨트롤 열거 팁, 현재 도구 schema 정보를 바로 알 수 있게 했습니다.
- `system_editor_state`, `system_editor_log`를 추가하고 `system_editor_control`도 확장해, Agent가 편집기 상태를 확인하고 Output을 읽거나 지우고, Dock과 하단 패널을 활성화하고, 팝업을 다루고, 편집기 화면을 캡처할 수 있게 했습니다.
- `system_project_files`와 `system_scene_tree`를 추가해 각각 프로젝트 파일 트리 조작과 현재 편집 장면 트리 조작을 담당하게 했습니다.
- 런타임 자동화 도구 `system_runtime_control`, `system_runtime_capture`, `system_runtime_input`, `system_runtime_step`을 추가해, 런타임 세션 제어, 스크립트 입력, 스크린샷, 입력 뒤 대기 뒤 스크린샷의 닫힌 흐름을 지원하게 했습니다.
- 편집기 스크린샷과 런타임 스크린샷의 출력 디렉터리를 지정할 수 있게 했고, `system_userdata_maintenance`를 추가해 플러그인이 관리하는 스크린샷과 캐시 파일을 먼저 미리 보고 그다음 정리할 수 있게 했습니다.
- `custom_tools/` 사용자 도구 자동 발견과 hot reload를 추가해, 적법한 스크립트를 `res://addons/godot_dotnet_mcp/custom_tools/`에 넣으면 Godot를 다시 시작하지 않아도 플러그인이 찾고, 도구 페이지에 출처, 상태, 다시 불러오기 필요 여부, 최신 오류를 보여주게 했습니다.
- Config 페이지에 더 많은 클라이언트를 위한 원클릭 설정과 설치 감지를 추가해, Claude Code CLI, Codex CLI/Desktop, Gemini CLI, OpenCode Desktop, Windsurf, Cline, Roo Code, Qwen Code, Cherry Studio를 다뤘습니다.
- 통합 도구 메타데이터를 추가해 Dock 도구 페이지와 MCP 도구 목록이 같은 이름, 설명, 분류, 동작, 내부 연결 정보를 쓰게 했습니다.
- 서비스 버전과 도구 schema 정보의 protocol facts 파일을 추가해 Agent와 클라이언트가 버전 변화를 구분하기 쉽게 했습니다.
- Home, 설정 페이지, 도구 페이지, 사용자 도구, 자체 진단, 도구 설명 같은 새 UI의 다국어 문구를 보강했습니다.
- Godot headless 플러그인 테스트 framework를 추가하고, 많은 도구 executor와 런타임 서비스 contract test, 배포 전 검증과 패키징 workflow도 보강했습니다.

### Changed

- Dock의 Home, 설정 페이지, 도구 페이지를 다시 정리했습니다. 첫 번째 탭 이름은 이제 `홈`이고, 서비스 상태, endpoint, 완전 재시작, 플러그인 자체 진단이 여기에 모였습니다.
- 설정 페이지를 더 직관적으로 바꿔, 지원 클라이언트는 설치, 제거, 열기 동작이 더 분명하게 보이고, 가능할 때는 구체적인 설치 위치도 보이게 했습니다.
- 도구 페이지를 통합 트리 표시로 바꿔, 상위 System 도구와 User Tool이 같은 방식의 현지화 이름, 설명, action 노드, 개수 통계, 내부 구현 연결을 갖게 했습니다.
- 이전 버전의 Intelligence였던 프로젝트, 장면, 스크립트, 런타임 진단, 인덱스 작업 흐름을 `system_*` 도구 이름으로 옮겼습니다.
- Agent를 위한 핵심 MCP 공개면을 상위 `system_*` 도구로 수렴시켰고, 하위 도구는 플러그인 내부에 남겼습니다.
- 원래의 Intelligence 도구 계층을 System 도구 계층으로 이름 바꾸고 정리해, 공개 `system_*` API 이름과 맞췄습니다.
- 큰 도구 executor를 더 작은 편집기, 스크립트, 애니메이션, 런타임, System, User, 공통 서비스, 각 도구 도메인 서비스로 나눠 유지보수와 테스트를 쉽게 했습니다.
- 런타임과 서버 내부를 더 분명한 HTTP, JSON-RPC, stdio, 도구 라우팅, reload, self-diagnostic, 런타임 제어 서비스로 나눠, 하나의 거대한 server 파일에 몰아두지 않았습니다.
- stdio 라우팅을 확장해 상위 `system_*` 도구와 HTTP 클라이언트가 같은 공개 도구 경로를 쓰게 했습니다.
- 플러그인 시작, 재시작, Dock 조정, 런타임 상태, 자체 진단, 설정 투영을 다시 만들어, 플러그인을 재시작한 뒤 더 쉽게 복구하고 실제 상태를 보고할 수 있게 했습니다.
- 사용자 도구 로드 흐름을 "도구 찾기, 디렉터리 새로 고침, 도구 스크립트 다시 불러오기, UI 새로 고침"의 단계로 나눠 상태 변화를 더 분명하게 했습니다.
- 외부 로그 수준을 `debug`, `info`, `warning`, `error`로 줄였습니다.
- tag 간 git 비교를 기준으로 변경 로그를 다시 만들어, 각 버전이 바로 이전 공개 버전과의 실제 변화만 기록하게 했습니다.
- README와 컴포넌트, UI, 테스트, 배포, 영속화, 인코딩 규격 문서를 v1.0 플러그인 형태에 맞춰 갱신했습니다.

### Removed

- 옛 공개 `intelligence_*` 도구 이름을 제거했습니다. 대부분의 작업 흐름은 이제 `system_project_state`, `system_runtime_diagnose`, `system_scene_analyze`, `system_script_analyze`, `system_bindings_audit` 같은 대응 도구로 옮겨졌습니다.
- 독립 `intelligence_project_advise` 제안 도구를 제거했습니다. 이제 Agent는 `system_help`, `system_project_state`, `system_editor_state`, 진단 도구, 장면과 스크립트 분석 도구로 먼저 상태를 보고 다음 도구를 스스로 고르면 됩니다.
- 낮은 수준 atomic tool domain을 주요 공개 작업 흐름 진입점에서 제거했습니다. 장면, 스크립트, 편집기, 런타임, 파일 시스템, 애니메이션, 노드, 리소스, 디버그 같은 하위 능력은 여전히 상위 도구와 도구 페이지 트리 뒤의 내부 구현으로 남아 있습니다.
- 옛 공개 Intelligence 도구 트리와 대응 문서 페이지를 제거하고, System 도구 트리와 갱신된 System 도구 설명으로 통일했습니다.
- 옛 권한 수준 화면과 홈의 고급 권한 설정을 제거해, 사용자가 더 이상 능력 등급을 직접 고를 필요가 없게 했습니다.
- 독립 공개 로그 수준으로서의 `trace`를 제거했고, 예전 입력은 호환 별칭으로 `debug`에 합쳤습니다.
- 옛 `user://` 루트 캐시 레이아웃을 현재 저장 모델에서 제거했습니다. 플러그인이 관리하는 스크린샷, 런타임 데이터, 로그, profile, 설정 교환 파일은 이제 모두 `user://godot_dotnet_mcp/` 아래에 두고, 명시적 유지보수 도구로 옛 파일을 정리합니다.
- 패키지에 함께 넣던 옛 스크립트 도구 총괄 진입점과 Intelligence 배포기 파일을 제거했습니다. 관련 능력은 System 도구 계층과 스크립트 서비스 구현으로 나뉘었습니다.

### Fixed

- HTTP와 stdio 두 경로에서 상위 `system_*` 도구 라우팅 일관성을 고쳐, 같은 공개 도구 묶음을 두 연결 방식 모두에서 쓸 수 있게 했습니다.
- 플러그인 재시작과 Dock 재생성 흐름을 고쳐, 재시작 뒤 설정을 더 안정적으로 유지하고, Dock model을 갱신하며, 진단 상태를 표시하게 했습니다.
- 런타임 서비스 종료와 포트 재사용 문제를 고쳐, 재시작 뒤 포트가 여전히 점유된 것처럼 보여 서비스 시작이 실패할 가능성을 낮췄습니다.
- `system_project_run`의 timeout 처리를 고쳐, 필요하면 오래 실행되는 장면을 자동으로 중지할 수 있게 했습니다.
- 도구 계층 재편 뒤 GDScript 진단과 스크립트 편집 보조 경로 접근 문제를 고쳤습니다.
- 사용자 도구의 추가, 수정, 삭제, 복원, 마지막 User 도구 삭제 뒤 정리 흐름을 고쳐, 도구 페이지가 Godot를 다시 시작하지 않아도 올바른 상태로 돌아오게 했습니다.
- 도구 메타데이터, 분류, 목록, 표시 정보 불일치를 고쳐, 도구 페이지와 MCP 도구 목록이 맞게 했습니다.
- headless 검증과 계약 테스트의 여러 빈틈을 고쳐, CI가 더 많은 플러그인 런타임, UI 표시, 도구 executor 동작을 덮도록 했습니다.

## [0.5.0] - 2026-03-19

### Added

- 비동기 GDScript 진단을 추가했습니다. `intelligence_script_analyze(include_diagnostics=true)`는 먼저 스크립트 구조를 돌려주고, 디스크에 저장된 파일을 바탕으로 백그라운드에서 `diagnostics`를 채우며, 첫 호출은 `pending`을 돌려줄 수 있습니다.
- 런타임 건강 요약과 자세한 자체 점검을 계층화했습니다.
  - `plugin_runtime_state(action=get_lsp_diagnostics_status)`는 유일한 상세 LSP 자체 점검 진입점으로, `loader / service / client`를 돌려줍니다.
  - `intelligence_project_state(include_runtime_health=true)`는 가벼운 `lsp_diagnostics` 건강 요약을 돌려줍니다.
- `stdio` 전송 계층(`plugin/runtime/mcp_stdio_server.gd`)을 추가해, 표준 `Content-Length` frame의 stdin/stdout MCP 채널을 지원하게 했습니다.
- 구조화 편집 능력을 확장했습니다.
  - `intelligence_scene_patch`에 `rename_node`, `update_property`를 추가
  - `intelligence_script_patch`에 `replace_method_body`, `delete_member`, `rename_member`를 추가
  - `intelligence_runtime_diagnose`에 `include_gd_errors`를 추가

### Changed

- `intelligence_script_analyze(include_diagnostics=true)`를 "구조 먼저 반환, 진단 나중 보강" 방식으로 바꿔, `publishDiagnostics`를 기다리느라 멈추지 않게 했습니다.
- `/api/tools`, MCP `tools/list`, Dock Tools를 모두 같은 visible tool set으로 만들었습니다. 호환 별칭은 여전히 호출할 수 있지만, 더 이상 주 표시 진입점은 아닙니다.
- GDScript LSP diagnostics 서비스는 이제 `tool_loader`가 일괄 소유하고, `reload_domain`, `reload_all_domains`, `soft_reload_plugin`을 거치며 생명주기를 맡아 오래된 인스턴스가 남지 않게 했습니다.
- 런타임 설명과 외부 문서를 `plugin_runtime_state`, `intelligence_project_state`, 현재 라우팅 동작을 중심으로 정리했습니다.

### Fixed

- `soft_reload_plugin`에서 가끔 "HTTP 서비스는 살아 있는데 도구 registry가 비어 있는" 문제가 생기던 것을 고쳤습니다. 이제 server/controller와 tool loader를 함께 다시 만들고, `/health`, `/api/tools`, `tools/call`이 서로 맞게 합니다.
- Tools tree가 재귀적으로 펼치고 접은 뒤 상태가 꼬이던 문제를 고쳤습니다. 루트 노드와 `atomic` 계층이 이제 통합 상태 모델에 따라 올바르게 복원되며, 계속 튀지 않습니다.

## [0.4.0] - 2026-03-17

### Added

- Intelligence 도구 계층을 추가해, 프로젝트 단위 추론과 조작을 위한 15개의 상위 도구를 네 분류로 제공했습니다.
  - **Project 6개**: `intelligence_project_state`, `intelligence_project_advise`, `intelligence_project_configure`, `intelligence_project_run`, `intelligence_project_stop`, `intelligence_runtime_diagnose`
  - **Scene 3개**: `intelligence_scene_validate`, `intelligence_scene_analyze`, `intelligence_scene_patch`
  - **Script 3개**: `intelligence_bindings_audit`, `intelligence_script_analyze`, `intelligence_script_patch`
  - **Index 3개**: `intelligence_project_index_build`, `intelligence_project_symbol_search`, `intelligence_scene_dependency_graph`
- Intelligence 도구와 하위 atomic 도구를 잇는 Atomic Bridge dispatch 계층을 추가했습니다.
- 사용자 정의 도구 통합을 추가했습니다. `custom_tools/` 아래 도구는 `user_*` 접두사를 쓰고, `handles()`, `get_tools()`, `execute()`를 구현해야 합니다.
- 플러그인 디렉터리 쓰기 보호(`PLUGIN_PROTECTED_PATHS`)를 추가해, 플러그인 자체 파일이 허가 없이 수정되지 않게 했습니다.
- 9개 언어의 Intelligence 문서 현지화를 추가했습니다. de/en/es/fr/ja/pt/ru/zh_cn/zh_tw입니다.

### Changed

- `Tools` 페이지 트리 구조를 다시 짜서, 최상위에 Intelligence 도구가 바로 보이고, 각 도구를 펼치면 의존하는 atomic tool chain을 볼 수 있으며, atomic tool도 다시 펼쳐 Action 노드까지 갈 수 있게 했습니다.
- `Shift` 재귀 펼침과 접기, 오른쪽 클릭 메뉴(도구 이름 복사, Schema 복사, 사용자 도구 삭제)를 추가했습니다.
- `MCPDebugBuffer` 로그 시스템을 다시 만들고, source 이름을 통일하고, 로그 수준(`trace/debug/info/warning/error`)을 늘리고, `tool_loader`, `intelligence`, `atomic_bridge`, `impl_*`의 주요 로그 지점을 채웠습니다.
- 저장소 디렉터리를 Godot Asset Library 규격의 `addons/godot_dotnet_mcp/` 배치로 재구성하고, 배포 ZIP 내용 제어용 `.gitattributes`를 추가했습니다.

### Removed

- `Tools` 페이지의 Profile preset 관리 UI를 제거했습니다. Profile 관리는 이제 `plugin_developer_*` 도구 모음에서 MCP를 통해 처리합니다.
- `Tools` 페이지의 사용자 도구 관리 UI를 잠시 제거했습니다. 사용자 도구 생성, 삭제, 복원은 현재 `plugin_evolution_*` 도구 모음으로 통합되며, 이후 버전에서 별도 UI가 돌아올 수 있습니다.

### Fixed

- `array` 타입 매개변수에 `items` 정의가 없을 때 발생하던 여러 MCP 도구의 `Invalid schema` 오류를 고쳤습니다. `node_call`, `undo_redo`, `group`, `signal`, `collision_shape` 같은 도구가 대상이었습니다.
- `editor_status`와 `node_transform`이 잘못된 매개변수 타입에 너무 느슨하게 반응하던 문제를 고쳐 입력 검증을 더 튼튼하게 했습니다.

## [0.3.0] - 2026-03-12

### Added

- Godot .NET / C# 작업 흐름 지원을 추가했습니다. `.csproj` 해석, 템플릿화 C# 스크립트 쓰기, 교차 파일 스크립트 참조 인덱스, `dotnet restore/build` 기반 구조화 진단을 포함합니다.
- 런타임과 플러그인 자체 점검 능력을 추가해, 런타임 오류 맥락, 컴파일 오류 위치, 플러그인 자체 점검 요약, 오류 타임라인, 건강 조회를 덮었습니다.
- 사용자 도구 관리 능력을 추가해, scaffold 버전과 호환성 점검, 감사 필터와 세션 식별자, 삭제 전 백업과 최근 복원 진입점을 다뤘습니다.
- 도구 사용 통계를 추가해 호출 횟수와 최근 호출 시간을 볼 수 있게 했습니다.
- 도구 설정 import/export를 추가해 profile과 disabled tools를 JSON으로 왕복할 수 있게 했습니다.
- 플러그인 구조, UI, 컴포넌트, 부록을 아우르는 완전한 기술 문서 체계를 추가했습니다.

### Changed

- Dock의 플러그인 자체 점검 요약을 `Server` 페이지 상단으로 모아, 페이지 사이의 중복 정보를 줄였습니다.
- `Tools` 페이지의 트리 상호작용과 정보 계층을 다시 짜서, 검색, tooltip, 상태 표시, 미리보기 패널, 분할선 드래그, profile 작업 흐름을 정리했습니다.
- 새로 생긴 분류, 도구 설명, 팁의 현지화 리소스를 보강했습니다.
- 공개 버전 번호를 `0.3.0`으로 올리고, 플러그인 메타데이터와 런타임 버전 문자열도 함께 맞췄습니다.

### Fixed

- 호환 executor 집계 때문에 중복 플러그인 등록되던 문제를 고쳐, `plugin_runtime`, `plugin_evolution`, `plugin_developer`의 세 독립 진입점을 유지했습니다.
- 상속 스크립트 hot reload가 불완전해 도구 도메인 로드가 빠지던 문제를 고쳐, `script` 도메인과 확장 도구의 안정적 발견을 되돌렸습니다.
- 플러그인 시작과 중지, 런타임 재로딩 중 HTTP 전송이 끊기던 문제를 고쳐, soft reload를 지연 예약으로 바꿨습니다.

## [0.2.0] - 2026-03-11

### Added

- 메인 프로젝트의 런타임 읽기 기능을 추가해, `debug_runtime_bridge`로 Godot 편집기 시작과 중지 뒤의 디버그 세션 상태를 추적할 수 있게 했습니다.
- 런타임 제어, 자동화 도구 관리, 개발자 진입점, 사용 안내를 포함한 더 완전한 플러그인 거버넌스 계층을 추가했습니다.
- 안정 사용, 자동화 확장, 개발 디버그 능력을 구분하는 플러그인 권한 수준과 승인 경계를 추가했습니다.
- `User` 분류 관리 지원을 추가해 사용자 측 확장 도구를 찾고, 감사하고, 정리하기 쉽게 했습니다.

### Changed

- 도구 그룹과 플러그인 분류를 다시 정리해, 하나의 도구 진입점이 너무 많은 동작을 노출하지 않게 하고, 발견성을 높였습니다.
- Dock UI와 문서를 단순화해, 좁은 폭에서 `Server`, `Config`, `Tools`가 더 잘 쓰이도록 했습니다.
- 더 많은 분류, 도구 설명, 팁의 다국어 내용을 보강해 번역되지 않은 표식을 줄였습니다.
- `README`, 중국어 README, 릴리스 문서를 맞춰, 처음 연결, 설치, 설정 흐름이 일관되게 했습니다.

### Fixed

- `Tools` 페이지가 접히고 다시 만들어질 때 생기던 `Tree blocked` / 빈 인스턴스 오류를 고쳐, Dock 사용 중 끊김과 연쇄 오류를 줄였습니다.

### Known Limitations

- 현재 런타임 읽기는 구조화된 상태와 생명주기 정보를 읽는 데 더 적합하고, Godot 원래 Output / Debugger 패널을 완전히 비추는 용도는 아닙니다.
- 프로젝트 안에 이미 같은 이름의 `MCPRuntimeBridge` Autoload가 있으면, 플러그인은 그 설정을 강제로 덮지 않으며 관련 런타임 읽기는 미설치로 보일 수 있습니다.

## [0.1.0] - 2026-03-11

### Added

- 첫 공개 릴리스입니다.
- Dock 기반 설정 UI와 도구 Profile 관리.
- 75개의 최상위 MCP 도구.
- 장면, 노드, 리소스, 스크립트, 애니메이션, 머티리얼, TileMap, 네비게이션, 물리, 오디오, UI 기능.
- Godot .NET / C# 장면 바인딩 분석과 export 멤버 감사.
- TileSet 최소 닫힌 루프 지원, `create_empty`와 `assign_to_tilemap`.
- 디버그 이벤트 버퍼와 기본 런타임 진단 읽기 도구.
- 관리되는 임시 장면 디렉터리와 장면 저장 경로.
- 상속을 고려한 리소스 타입 필터.
- 설치와 배포 패키징 문서.

### Known Limitations

- `/root/...` 경로 호환은 패치했지만, 최종 동작은 여전히 플러그인 재시작 뒤의 안정성에 달려 있습니다.

[Unreleased]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v1.3.0...HEAD
[1.3.0]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v1.2.0...v1.3.0
[1.2.0]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v1.1.2...v1.2.0
[1.1.2]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v1.1.1...v1.1.2
[1.1.1]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v1.1.0...v1.1.1
[1.1.0]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v1.0.1...v1.1.0
[1.0.1]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v1.0.0-pre3...v1.0.0
[1.0.0-pre3]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v1.0.0-pre2...v1.0.0-pre3
[1.0.0-pre2]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v1.0.0-pre1...v1.0.0-pre2
[1.0.0-pre1]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v0.5.0...v1.0.0-pre1
[0.5.0]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v0.4.0...v0.5.0
[0.4.0]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v0.3.0...v0.4.0
[0.3.0]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/LuoxuanLove/godot-dotnet-mcp/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/LuoxuanLove/godot-dotnet-mcp/releases/tag/v0.1.0
