# v1.4.0 Refactor Progress Tracker

This tracker turns the v1.4.0 protocol refactor plan into an execution checklist. It is intentionally narrower than the full plan: each row should map to PR-sized work that can be reviewed, tested, and merged into the v1.4 refactor branch.

Status legend:

- `Done`: implemented, validated, reviewed, and merged into the v1.4 refactor branch.
- `Partial`: useful work has landed, but the plan item still has required follow-up work.
- `Pending`: no substantial implementation has landed yet.
- `Blocked`: blocked by a known external prerequisite.

## Current Phase

The v1.4.0 line is in the final protocol-surface and service-convergence phase. The refactor target is now MCP 2025-11-25 conformance by default, with the earlier 2025-06-18 alignment retained as a compatibility foundation. The branch has strong test scaffolding, an explicit MCP 2025-11-25 aggregate conformance gate, and broad protocol/UI metadata coverage, but it has not yet completed richer Streamable HTTP polling/resume semantics, atomic bridge thinning, or internal service convergence.

## Progress Matrix

| Plan axis | Status | Landed evidence | Remaining work |
|---|---|---|---|
| Test guardrails first | Done | Contract case manifest, manifest-driven harness selection, 2025-11-25 conformance metadata, protocol facts parity policy, changelog section order and duplicate-section checks | Keep newly added protocol/UI/transport gates in the manifest as future cases land |
| MCP 2025-11-25 conformance baseline | Done | v1.4 plan now targets MCP 2025-11-25 while keeping 2025-06-18 work as compatibility foundation; manifest and real-loader contracts guard public tool names against the 1-128 character ASCII letter, digit, `_`, `-`, and `.` naming profile; `mcp_2025_11_25_conformance_contracts` aggregates lifecycle, JSON-RPC, Tools, Resources, Prompts, schema metadata, Streamable HTTP, stdio, and Dock metadata gates | Keep expanding transport-specific contracts as richer Streamable HTTP polling/resume semantics land |
| Canonical resources | Done | Guide, project/editor state, activity, and tool catalog resource URIs with contract coverage | Add 2025-11-25 icons/title metadata and reuse a future catalog snapshot service instead of local resource builders |
| Structured tool results | Done | `structuredContent` in tool-call results and output schemas in tool list/presentation data | Add schema dialect policy and keep text JSON compatibility until client support is broad enough |
| Prompt argument discipline | Done | Unknown prompt arguments are rejected with allowed argument metadata | Remove stale prompt guidance that still tells clients to start with `system_help` |
| Public tool surface guardrails | Partial | Public category allow-list, `system` / `user` category contracts, and per-tool removal guards for activity and scene validation aliases | Keep adding per-tool replacement errors as public tools are retired |
| Streamable HTTP and stdio transport | Partial | HTTP and stdio share JSON-RPC/object-format contracts, malformed Content-Length guards, newline-delimited stdio default, `/mcp` required content-type/version/session/Accept/Origin checks, JSON-RPC response POST 202 handling, finite POST SSE responses for JSON-RPC requests that explicitly prefer `text/event-stream`, HTTP audit session/version projection, GET SSE event metadata, bounded per-session `Last-Event-ID` replay, explicit matched/stale/unknown resume status metadata, Content-Length-free long-lived SSE stream opening, heartbeat comments, queued server-to-client event delivery, `transport_mode=sse` diagnostics, and disconnected stream session history | Complete richer polling/resume semantics and the final transport conformance gate |
| System help protocol guidance | Partial | `system_help` now explains Resources, Prompts, and Tools separation | Keep `system_help` legacy calls as replacement errors while discovery lives in Resources and Prompts |
| Tool catalog diagnostics | Partial | Search diagnostics report filter warnings, available values, and suggested queries | Move public catalog discovery from `system_tool_catalog` to catalog resources |
| User-tool recovery diagnostics | Partial | User-tool runtime failures include diagnostic codes, recommended actions, and next tool hints | Feed diagnostics into future resource/catalog snapshots |
| Version baseline | Blocked | v1.4 metadata and release-note baseline branch exists | Required policy still depends on the trusted workflow change being present on `dev` |
| Public tool debt cleanup | Partial | `system_tool_activity`, `system_scene_validate`, and `system_scene_analyze` are removed from the public surface with replacement guards | Remove `system_help`, `system_tool_catalog`, duplicate plugin/editor-log tools, and remaining old aliases with replacement guards |
| Single catalog fact source | Partial | `ToolCatalogManifest` now owns built-in tool entries, derived category lists, domain metadata, public category exposure, and removed public tool guards; registry/manifest/loader adapters read from it; catalog snapshots, catalog resources, presentation, search, `/api/tools`, Dock model metadata, and Tools tab preview/search/schema-copy consumers now reuse the manifest/snapshot path | Migrate remaining low-level Tools tab category/domain preview edges and future context actions onto the manifest/snapshot path |
| 2025-11-25 UI metadata adaptation | Partial | Dock model and Tools tab presentation consumers now carry protocol-aligned descriptions, titles, icons, annotations, JSON Schema dialects, input schemas, output schemas, and exposure states from the shared snapshot; Dock model projects MCP Resources, Resource Templates, and Prompts metadata; Dock Resources and Prompts tabs render protocol catalog counts, identifiers, descriptions, icons, kind/MIME/argument summaries, copy-ID affordances, resource read previews, prompt argument inputs, `prompts/get` previews, and generated prompt copy actions from that projection | Finish any remaining Tools tab output-schema/icon rendering edges and future context actions |
| Atomic bridge demotion | Partial | AtomicBridge write-policy, dependency-reference, protected-path, issue helpers, file-collection helpers, executor loading, executor cache, runtime context wiring, sync/async dispatch, and support/runtime/dispatch composition are split into dedicated support/runtime/execution services while the bridge facade API stays stable | Continue reducing compatibility helper breadth and route future callers through the internal execution service directly |
| Old monolith removal | Partial | Audio, animation, signal, TileMap, UI, filesystem, node, project, resource, scene, group, geometry, material, lighting, navigation, particle, physics, and shader now use split executors as their only stable entries, with root monolith absence guards | Continue with debug/editor compatibility entries and any remaining root-level domain monoliths |
| Internal service convergence | Partial | Public tool exposure policy, execution observation, executor runtime helpers, atomic execution composition, loader status snapshots, loader diagnostic incident handling, registry entry indexing, loader runtime-context wiring, and loader catalog/domain projections are split from `MCPToolLoader`; current services still have duplicated routing and lifecycle plumbing | Unify HTTP/stdio routing, consolidate remaining context bags, and slim plugin lifecycle wiring |

## Next PR Queue

1. Complete Streamable HTTP beyond the current `/mcp` version/session/Accept/Origin checks, finite POST SSE responses, long-lived GET SSE stream lifecycle, bounded resumable event replay, explicit resume status metadata, and queued server-to-client event delivery by adding richer polling/resume semantics and final transport conformance contracts.
2. Finish remaining Tools tab output-schema/icon rendering edges and future context actions.
3. Adapt any remaining Dock/Tools UI models to render from shared catalog snapshot metadata without private tree facts.
4. Keep Resource/Prompt catalog previews aligned with protocol metadata as new resources, templates, and prompts are added.
5. Migrate remaining Dock and Tools/RPC catalog consumers onto `ToolCatalogManifest` / `ToolCatalogSnapshotService`.
6. Continue monolith cleanup with debug/editor compatibility entries and any remaining root-level domain monoliths.
7. Continue demoting `atomic_bridge` after moving support/runtime/dispatch composition behind the internal execution service; remaining work is to reduce compatibility helper breadth and route future callers through the service directly.
8. Continue splitting `MCPToolLoader` responsibilities after the public-surface policy, execution-observer, runtime-manager, status, diagnostics, registry-entry, runtime-context, and catalog-projection extractions.
9. Continue internal service convergence after catalog consumers share the manifest/snapshot path.

## Tracking Rules

- Every breaking removal must include a replacement method or resource URI in the returned error.
- Every removed public tool must have tests proving it is absent from `tools/list`, absent from visible catalog resources, and not callable as a successful action.
- Prompt guidance must describe the resource-first path and must not reference removed discovery tools as the starting point.
- The default v1.4 endpoint is `http://127.0.0.1:3000/mcp` as MCP Streamable HTTP; legacy `/api/tools`, `/health`, JSON-only POST behavior, and `Content-Length` stdio must be labeled as compatibility surfaces.
- UI catalog models must consume shared catalog snapshot metadata instead of inventing separate private catalog facts.
- Changelog entries for this line must keep the order `Added / Changed / Fixed / Documentation / Internal` and must not duplicate section headings inside a version.
- A PR is not ready to merge into the v1.4 refactor branch until local validation, relevant GitHub checks, all conversations, and the Codex review gate are complete.
