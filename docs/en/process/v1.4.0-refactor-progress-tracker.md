# v1.4.0 Refactor Progress Tracker

This tracker turns the v1.4.0 protocol refactor plan into an execution checklist. It is intentionally narrower than the full plan: each row should map to PR-sized work that can be reviewed, tested, and merged into the v1.4 refactor branch.

Status legend:

- `Done`: implemented, validated, reviewed, and merged into the v1.4 refactor branch.
- `Partial`: useful work has landed, but the plan item still has required follow-up work.
- `Pending`: no substantial implementation has landed yet.
- `Blocked`: blocked by a known external prerequisite.

## Current Phase

The v1.4.0 line is in the final service-convergence phase. The refactor target is MCP 2025-11-25 conformance by default, with the earlier 2025-06-18 alignment retained as a compatibility foundation. The branch has strong test scaffolding, an explicit MCP 2025-11-25 aggregate conformance gate, broad protocol/UI metadata coverage, and release-contract Streamable HTTP coverage. Remaining work is concentrated in deeper atomic bridge thinning, `MCPToolLoader` service convergence, and any follow-up transport behavior that goes beyond the current release contract suite.

## Progress Matrix

| Plan axis | Status | Landed evidence | Remaining work |
|---|---|---|---|
| Test guardrails first | Done | Contract case manifest, manifest-driven harness selection, 2025-11-25 conformance metadata, protocol facts parity policy, changelog section order and duplicate-section checks | Keep newly added protocol/UI/transport gates in the manifest as future cases land |
| MCP 2025-11-25 conformance baseline | Done | v1.4 plan now targets MCP 2025-11-25 while keeping 2025-06-18 work as compatibility foundation; manifest and real-loader contracts guard public tool names against the 1-128 character ASCII letter, digit, `_`, `-`, and `.` naming profile; `mcp_2025_11_25_conformance_contracts` aggregates lifecycle, JSON-RPC, Tools, Resources, Prompts, schema metadata, Streamable HTTP, stdio, and Dock metadata gates | Keep expanding transport-specific contracts as richer Streamable HTTP polling/resume semantics land |
| Canonical resources | Done | Guide, project/editor state, activity, editor log, and tool catalog resource URIs with contract coverage; Resources and Resource Templates now carry MCP 2025-11-25 title/icon metadata and catalog resources reuse the shared snapshot path | Keep future resource additions on the shared snapshot/resource-service path |
| Structured tool results | Done | `structuredContent` in tool-call results, input/output schemas in tool list and presentation data, and JSON Schema 2020-12 dialect policy in protocol contracts | Keep text JSON compatibility until client support is broad enough |
| Prompt argument discipline | Done | Unknown prompt arguments, invalid argument types, and invalid `include_runtime` values are rejected with allowed argument metadata; prompt guidance starts from Resources and Prompts | Keep future prompts resource-first and schema-disciplined |
| Public tool surface guardrails | Done | Public category allow-list, `system` / `user` category contracts, removed-public-tool policy, removed tool replacement metadata, and per-tool removal guards for activity, help/catalog discovery, scene aliases, plugin reload/update, editor log, resource, and debug compatibility entries | Add replacement guards whenever future public tools are retired |
| Streamable HTTP and stdio transport | Done | HTTP and stdio share JSON-RPC/object-format contracts, malformed Content-Length guards, newline-delimited stdio default, `/mcp` required content-type/version/session/Accept/Origin checks, JSON-RPC response POST 202 handling, finite POST SSE responses for JSON-RPC requests that explicitly prefer `text/event-stream`, HTTP audit session/version projection, GET SSE event metadata, bounded per-session `Last-Event-ID` replay, explicit matched/stale/unknown resume status metadata, Content-Length-free long-lived SSE stream opening, heartbeat comments, queued server-to-client event delivery, `transport_mode=sse` diagnostics, disconnected stream session history, `DELETE /mcp` session termination that clears resumable history and closes matching active SSE streams, and aggregate conformance semantic checks that execute the registered HTTP contracts and require their structured Streamable HTTP coverage flags | Keep any future transport behavior beyond this release contract suite covered by explicit contracts |
| System help protocol guidance | Done | `system_help` is removed from the public tool surface, legacy calls return replacement guidance, and current prompt/docs guidance explains Resources, Prompts, and Tools separation | Keep new onboarding text resource-first |
| Tool catalog diagnostics | Done | Search diagnostics report filter warnings, available values, and suggested queries; public catalog discovery lives in catalog resources while `system_tool_catalog` legacy calls return replacement guidance | Keep future diagnostics aligned with catalog resources |
| User-tool recovery diagnostics | Done | User-tool runtime failures include diagnostic codes, recommended actions, and next tool hints; compatibility reporting and catalog snapshots expose user-tool status through structured diagnostics | Keep new recovery diagnostics structured and resource/catalog friendly |
| Version baseline | Done | `plugin.cfg`, protocol facts, .NET bridge metadata, localized changelogs, release-note sources, and initialize `serverInfo.description` are aligned to `1.4.0` / MCP `2025-11-25` | Keep the final dev PR gated by the trusted workflow and release-policy checks |
| Public tool debt cleanup | Done | `system_help`, `system_tool_activity`, `system_tool_catalog`, `system_scene_validate`, `system_scene_analyze`, `system_plugin_reload`, `system_plugin_update`, `system_editor_log`, `resource_manage`, `debug_log`, and other retired aliases are absent from the public surface or visible catalog resources and return replacement guidance where legacy calls remain accepted | Keep future breaking cleanup narrow and guarded |
| Single catalog fact source | Partial | `ToolCatalogManifest` now owns built-in tool entries, derived category lists, domain metadata, public category exposure, and removed public tool guards; registry/manifest/loader adapters read from it; catalog snapshots, catalog resources, presentation, search, `/api/tools`, Dock model metadata, and Tools tab preview/search/schema-copy/action-label consumers now reuse the manifest/snapshot path; Tools tab metadata lookup, atomic previews, and refresh signatures ignore raw `tools_by_category` fallback data when shared presentation metadata is available | Migrate remaining low-level Tools tab category/domain preview edges and future context actions onto the manifest/snapshot path |
| 2025-11-25 UI metadata adaptation | Done | Dock model and Tools tab presentation consumers now carry protocol-aligned descriptions, titles, icons, annotations, JSON Schema dialects, input schemas, output schemas, and exposure states from the shared snapshot; Tools tab rows render protocol icon metadata from the shared presentation path, action rows/previews consume shared label and description keys, and previews/context menus expose shared input and output schema metadata; Dock model projects MCP Resources, Resource Templates, and Prompts metadata through a configured catalog projection service; Dock Resources and Prompts tabs render protocol catalog counts, identifiers, descriptions, icons, kind/MIME/argument summaries, copy-ID affordances, resource read previews, prompt argument inputs, `prompts/get` previews, generated prompt copy actions, preview metadata, and bounded invalid-icon fallbacks from that projection | Keep future Tools tab context actions on shared catalog snapshot metadata |
| Atomic bridge demotion | Partial | AtomicBridge write-policy, dependency-reference, protected-path, issue helpers, file-collection helpers, executor loading, executor cache, runtime context wiring, sync/async dispatch, and support/runtime/dispatch composition are split into dedicated support/runtime/execution/helper services; production system impls now receive a dedicated helper service for data extraction, file collection, issue, and dependency helpers; `AtomicBridgeExecutionService` is kept execution-only by source guards; the bridge facade has been slimmed to execution, context, success, and error operations with helper-forwarding and source-level anti-backflow guard coverage | Keep future helper/collection behavior on `AtomicBridgeHelperService` and continue preventing production impls or bridge facade code from depending on helper methods directly |
| Old monolith removal | Done | Audio, animation, signal, TileMap, UI, filesystem, node, project, resource, scene, group, geometry, material, lighting, navigation, particle, physics, and shader now use split executors as their only stable entries, with root monolith absence guards; `tool_root_monolith_closure_contracts` allows only `base_tools.gd` plus the intentional debug/editor compatibility wrappers at the tools root, and debug/editor wrapper contracts prove those files only extend canonical split executors | Keep any future root compatibility wrapper explicit and guarded |
| Internal service convergence | Partial | Public tool exposure policy, execution orchestration, execution observation, executor runtime helpers, atomic execution composition, loader status snapshots, loader diagnostic incident handling, registry entry indexing, loader runtime-context wiring, loader frame tick/user-runtime maintenance, loader catalog/domain projections, loader mutable state/context projection, loader tool-access provider policy glue, loader LSP diagnostics adapter lifecycle, loader execution-context/failure-envelope callbacks, loader catalog/reload/runtime/lifecycle context assembly, loader-specific context wiring, stdio tool-router/resource/prompt context wiring, and plugin entrypoint lifecycle orchestration are split from their facades; unused loader reload state forwarding methods are removed, and source guards keep context map assembly out of loader and transport facades | Continue slimming remaining config/reload coordinator context wiring and keep future HTTP/stdio service contexts on dedicated builders |

## Next PR Queue

1. Continue internal service convergence by slimming remaining config/reload coordinator context wiring and keeping HTTP/stdio service contexts on dedicated builders.
2. Finish remaining low-level Tools tab private-tree fact-source cleanup and keep future context actions on shared catalog snapshot metadata.
3. Keep atomic helper/collection behavior on `AtomicBridgeHelperService` and prevent future backflow into `AtomicBridgeExecutionService` or the bridge facade.
4. Keep monolith cleanup closed by maintaining the aggregate root monolith guard and explicit debug/editor compatibility-wrapper contracts.
5. Keep Streamable HTTP behavior beyond the current release contract suite covered by explicit transport contracts before expanding it.

## Tracking Rules

- Every breaking removal must include a replacement method or resource URI in the returned error.
- Every removed public tool must have tests proving it is absent from `tools/list`, absent from visible catalog resources, and not callable as a successful action.
- Prompt guidance must describe the resource-first path and must not reference removed discovery tools as the starting point.
- The default v1.4 endpoint is `http://127.0.0.1:3000/mcp` as MCP Streamable HTTP; legacy `/api/tools`, `/health`, JSON-only POST behavior, and `Content-Length` stdio must be labeled as compatibility surfaces.
- UI catalog models must consume shared catalog snapshot metadata instead of inventing separate private catalog facts.
- Changelog entries for this line must keep the order `Added / Changed / Fixed / Documentation / Internal` and must not duplicate section headings inside a version.
- A PR is not ready to merge into the v1.4 refactor branch until local validation, relevant GitHub checks, all conversations, and the Codex review gate are complete.
